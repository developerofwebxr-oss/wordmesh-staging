# WordMesh — Execution Spec (CLAUDE.md)

Read this whole file before touching code. Strategy is LOCKED (2026-07-05). Do not reopen "Locked decisions" — execute them.

## What this is
A live, walkable 3D network of the Superhero.com (aeternity) word-token economy. WebXR, one link, runs on desktop / mobile touch / Quest VR / AR passthrough. Word-tokens are glowing nodes (size/brightness = Trendminer price/volume). Creators and `.chain` users are nodes. Real buys/tips travel as light-pulses down lines between nodes. Erlang aesthetic: message-passing mesh that self-heals when nodes drop.

## Locked decisions — do not reopen
1. Live network floor, not ambient drift. The mesh visibly reacts to trade events. Nodes flare, dim, lines reroute.
2. No audio mechanics. No calls. Default silent. Erlang is expressed structurally, never as telephony.
3. Two distinct verbs, never conflated: BUY a word (Trendminer bonding-curve market) vs TIP a post/creator (tipping contract, social gift). Words are not tipped. Buys thicken/brighten word-nodes; tips draw person→person support-lines.
4. Non-custodial, reads-first. Rendering runs on free reads (REST/WS/node). Writes (buy/tip) ONLY via Superhero Wallet handoff (QR to phone / deep-link). Never hold keys. Writes are phase 3.
5. Data-adapter seam. All data flows through a `DataSource` interface. `MockSource` and `SuperheroSource` interchangeable. Engine chain-agnostic.
6. Replay layer. Real live volume is tiny (<$1k/day). Under the live WS feed, run a time-compressed replay of the last ~7 days of real trades, clearly labeled (dimmer/cooler pulses). The room must always breathe.
7. Presence privacy line (phase 2): other users appear as callsign (.chain name) + words they explicitly claim. NEVER visualize holdings/portfolio.
8. Single-file phase 0. One `index.html`, Three.js from CDN, GitHub Pages. No build step until it hurts.
9. Human nodes are embodied and mobile. `.chain` users = pill-shaped avatars with cut-sphere faces (Nostr XR stage style) + floating callsign. Locomotion: walk/run on an implied plane AND free flight (fly default in a 3D mesh; smooth locomotion + snap-turn in VR, WASD+mouse desktop, touch+gyro mobile).
10. Free to roam, paid to interact (phase 4). Seeing/walking/flying/discovering always free. Economic interactions cost AE, settle on-chain non-custodially: payload-tagged spend txs to dev `.chain` wallet (client verifies via middleware read; chain is the store, no backend accounts), plus one small Sophia split-contract for avatar gifts (~95% recipient / ~5% dev). Never paywall movement or visibility.

## Architecture
- Client: static, GitHub Pages. Three.js + WebXR. Mode switcher: desktop (orbit/WASD) / mobile (touch+gyro) / VR (Quest) / AR passthrough. Use webxr-threejs + threejs-visual-polish skills if available (guidance, not deps).
- Backend: none needed — CORS probe (2026-07-06) confirmed all endpoints below are CORS-open (`Access-Control-Allow-Origin: *` or origin-echoed) for a static third-party client. No Railway proxy built.
- Data endpoints (probed 2026-07-06 from superhero.com's production bundle + curl with a forged `Origin: https://developerofwebxr-oss.github.io` header):
  - `SUPERHERO_API_URL` = `https://api.superhero.com` — Trendminer REST. `/api/tokens?order_by=trending_score&order_direction=DESC&limit=100&page=1` (trending list, price/market_cap/holders; **limit hard-caps at 100** — server 400s above that); `/api/transactions?includes=token&limit=100&page=N` (paginated buy/sell/create_community history, used for replay).
  - `SUPERHERO_WS_URL` = `https://api.superhero.com` — Socket.IO (not raw WS), connected via `socket.io-client`. Events: `token-created`, `token-updated`, `token-transaction` (live buy/sell — the one we subscribe to), `token-history::<sale_address>`. No per-token subscribe needed for the global feed.
  - `NODE_URL` = `https://mainnet.aeternity.io`, `MIDDLEWARE_URL` = `https://mainnet.aeternity.io/mdw` (phase 2, unused so far).
  - `WALLET_URL` = `https://wallet.superhero.com` (phase 3, unused so far).
  - Real token universe is small: 51 word-tokens exist today (`meta.totalItems`), market caps ranging ~144 to ~1.6M AE — spans too many orders of magnitude for the mock-tuned size formula, see Status log.

### DataSource interface (keep this exact seam)
```js
// getSnapshot(): Promise<{tokens: [{word, price, volume24h, holders?, candles?}]}>
// subscribe(cb): live events {type: 'buy'|'sell'|'tip', word?, from?, to?, amount, ts}
// getReplay(days): Promise<event[]>
```
MockSource generates plausible data + Poisson-timed events. SuperheroSource implements the same against REST/WS. Swap via `?data=mock|live`.

## Performance budget (Quest = floor)
72fps Quest. Word-nodes via InstancedMesh (one draw call), cap ~300 visible, LOD: distant nodes = point sprites, no labels. All lines in one LineSegments buffer, rebuilt at most 1x/s; pulses are shader/uv animations. Labels: billboarded canvas textures, only within ~8m, pooled. No per-frame allocations in the render loop.

## Phases + acceptance
### Phase 0 — mock mesh demo (ship same-day)
Single index.html on Pages. MockSource. ~150–300 word-nodes in a 3D constellation around spawn, related-word lines, event pulses every few seconds, node flare/dim, and the Erlang beat: every ~60s one node dies and the mesh visibly reroutes. HUD: project name + "MOCK DATA" badge + mode switcher.
Done when: loads from Pages link; 72fps in Quest browser; usable on desktop mouse + phone touch; a 30s capture looks alive.
### Phase 1 — real data
Devtools probe → record real endpoints + CORS verdict here. SuperheroSource (REST snapshot → sizes; WS → live pulses; history → replay). Railway proxy only if CORS forces. Live pulses distinct from replay.
### Phase 2 — identity & presence
.chain resolve → embodied avatar (pill body, cut-sphere face, callsign, walk/run/fly), creator nodes, bot-presence then LiveKit behind the seam. Respect privacy line.
### Phase 3 — writes
Select node → QR → Superhero mobile app deep-link → buy/tip. Client never signs. Confirmation → pulse.
### Phase 4 — in-world economy
Free-to-roam absolute. Paid: cosmetics (`wm:glow:7d`), beacons (`wm:beacon:<word>:<ttl>`), orbit slots, avatar gifts (Sophia 95/5). Moderation on user text. Chain is the store.
### Phase 4+ revenue
B2B placements, weekly auctions, market-night events, semantic ad inventory (affiliate, Zesty programmatic, direct placements.json, brand-owned words with GLTF gated to verified owners). Ad integrity: every ad labeled; ad money never affects node size/brightness/position; no ads on avatars/.chain identities; billboards excluded from pulse grammar.

## Session workflow
- Sonnet phase 0–1; Opus for Quest perf / phase-2 presence / stubborn data. Strategy settled.
- Read this file first, check Status log, do the next unchecked criterion, update the log. Verify on-device claims honestly.

## Status log
- 2026-07-05: Strategy locked. Docs created. Repo: github.com/developerofwebxr-oss/wordmesh (Pages enabled). Endpoints unprobed. Phase 0 not started.
- 2026-07-05: Phase 0 shipped. Single index.html: MockSource (Poisson trades + ~60s death/respawn beat), 220 InstancedMesh word-nodes with k-NN related-word edges (single LineSegments buffer), pooled InstancedMesh pulses, node flare/dim, LOD (near = instanced spheres, far = point sprites), pooled billboarded labels (~8m), starfield backdrop, ACES tone mapping + bloom (flat only) + fog, desktop hold-drag+WASD, mobile touch+joystick+gyro, VR (smooth move + 30° snap-turn) and AR (passthrough, shell suppressed) via raw WebXR session lifecycle with sessionend cleanup. Verified in a Chrome tab via extension scripting: scene graph populates correctly, mesh/edges/pulses/flares render correctly, HUD (title, MOCK DATA badge, mode switcher, Fly/Free-look toggles) renders correctly. Fixed two real bugs found during verification: (1) AR sessions weren't clearing scene.background/fog, which would have blocked passthrough; (2) the far-LOD point layer's unclamped additive blending could blow out under many overlapping distant nodes — clamped and switched to normal blending. Untested/unverifiable this session: real Quest fps (no headset available), real phone touch/gyro (no device available). The automation browser tab used for self-verification runs backgrounded (`document.visibilityState: 'hidden'`), which freezes `requestAnimationFrame` — screenshots were produced by forcing one real synchronous render per capture rather than reading the live animation loop; this is a testing-harness limitation, not app behavior, and is worth reconfirming once on a normal foregrounded tab/device.

- 2026-07-06: Phase 0 movement/legibility polish, from on-device feedback ("movement feels dingy and off", "mesh reads as anonymous dots"). Movement root causes fixed: no velocity smoothing (instant snap start/stop instead of glide) — added frame-rate-independent exponential smoothing (`1-exp(-dt/0.15)`); base/sprint speed too slow for the field scale (1.4/2.8 → 5.0/11.0, VR 3.6/7.5); digital WASD diagonal moved at sqrt(2)x speed — clamped combined input to unit magnitude; joystick had no deadzone/curve, so tiny touch jitter caused drift — added a 0.12 deadzone + quadratic ease-in applied radially; gyro free-look set yaw/pitch from *absolute* compass heading/tilt, which snapped the view to an unrelated orientation the instant it was enabled and drifted independently of how far you'd actually turned — rewritten to calibrate a baseline on enable and apply *delta* rotation only (up/down sign is a best-effort guess, unverified on real hardware); there was no dedicated vertical axis at all (only pitch-and-fly-forward) — added Space/Ctrl on desktop and two on-screen ascend/descend buttons on mobile, decoupled from look direction. Labels: added a second, larger, persistent label pool (20 slots) for the top-volume words, ranked/reassigned every 2.5s (not per frame), scaling up with distance (clamped) so they stay legible across the mesh and fading in/out near the camera and near the fog line; smaller nodes keep the original ~8m proximity-only pool, now skipping any node already carrying a persistent label. Verified via Chrome extension scripting (same backgrounded-tab caveat as above — forced renders, not the live loop): top labels render correctly ranked by volume with distance-based scale/fade, mobile joystick + new vertical buttons lay out without overlap. Movement feel itself (smoothing, speed, deadzone) verified by re-deriving the shipped formulas and checking them at representative inputs, not by feel — genuinely confirming "glide" quality needs an on-device pass.

- 2026-07-06: Phase 1 shipped — real data. Probe verdict: LIVE + CORS-OPEN (see Architecture section above for concrete endpoint values and evidence), so `SuperheroSource` is a direct client-side fetch/Socket.IO client, no backend. Implementation: `getSnapshot()` hits `/api/tokens` (capped at limit=100, matches the real ~51-token universe) mapping symbol→word, `price_data.ae`→price, and `market_cap`→a `marketCap` field used for node size (raw `price` is fractions of an AE cent and doesn't differentiate visually — market cap, still a real price-derived metric, does); `trending_score` stands in for the 24h-volume field this endpoint doesn't expose, driving brightness. `subscribe(cb)` opens a Socket.IO connection to `api.superhero.com` and listens for `token-transaction`, mapping `sale_address` back to the node's word via a map built at snapshot time. `getReplay(days)` paginates `/api/transactions` filtering to `buy`/`sell` (drops `create_community`) until it passes the cutoff timestamp, then the render loop compresses those real trades into a continuously-looping ~90s playback of dimmer/cooler/slower pulses (existing `replay`-flagged pulse rendering was already built in Phase 0 but never wired to a real data source until now) — kept deliberately distinct from genuine live pulses (which are brighter, faster, and update the HUD trade-info panel; replay pulses do neither). `?data=live` swaps in via URL param, falls back to MockSource with a console warning if the live snapshot fetch fails/times out (8s), default `?data=mock` path untouched. Real market caps span ~144 to ~1.6M AE — several orders of magnitude wider than the range the Phase 0 log-scale size/brightness formulas were tuned for, so live mode normalizes size/brightness by log-scaling within the current snapshot's actual min/max rather than reusing the mock-tuned constants (mock mode is unaffected, still uses the original formulas). The Erlang death/respawn beat keeps a live node's real word/price/market-cap through a respawn rather than inventing a replacement, since the real token universe is small and fixed by symbol (no spare-word reservoir needed). Verified via a locally-served static copy of index.html (the Chrome extension's navigate tool refuses `localhost`/`file://` — likely a built-in SSRF/local-file guard — so verification ran through the Preview tool's managed browser instead, scripted the same way via `preview_eval`/JS, not manual clicking): `?data=mock` unchanged (220 nodes, MOCK DATA badge); `?data=live` shows 51 real nodes with real names/prices/market caps, node sizes now spread across the full visual range (43 distinct scale values observed, vs. all-51-saturated-identical before the log-normalization fix), LIVE DATA badge, WS connects successfully (real socket id logged), and the replay layer fires naturally (15 active dimmer/slower pulses observed within seconds of load, all correctly flagged). No genuine live buy/sell trade was observed firing during the verification window — consistent with the platform's documented <$1k/day real-time volume — so the live-pulse rendering path (vs. replay) was additionally confirmed by dispatching one synthetic same-shaped event through the real `onDataEvent` callback, showing it renders faster/brighter and distinct from the concurrently-running replay pulses. Screenshots: `verification/wordmesh-phase1-live-mesh.png`, `verification/wordmesh-phase1-mock-unaffected.png`.

- 2026-07-09: Cosmetic pass — readable labels + 3D nodes (rendering/visual only, per skill `threejs-visual-polish`; data flow, mock generator, WS/REST seams, camera/locomotion, and the Screen/VR/AR switcher untouched). Three fixes, done in order:
  **Fix 1 (labels never clip):** replaced the two old fixed-canvas label pools (`paintLabel`/`paintTopLabel`, 256×64 and 320×88 px) with one auto-fit system — each label's canvas is sized in `paintLabelAutoFit()` from `ctx.measureText()` at devicePixelRatio (+ padding + dot space), so the pill can never be narrower than its text; the sprite is then scaled by the canvas's own aspect ratio at a fixed world-space height. Tickers over 12 chars truncate to 9 chars + a real ellipsis (`LITCHSUPERHERO` → `LITCHSUPE…`, confirmed exact match against a live example). Screen-space overlap is resolved every frame in priority order (`screenHalfExtent()` projects perspective-correct pixel half-extents; `boxesOverlap()` hides a lower-priority label behind a higher one, and it reappears on its own once the camera moves enough to clear). Billboarding was already automatic (`THREE.Sprite`) — confirmed, not touched.
  **Fix 2 (label↔node link + declutter):** `LABEL_TOP_K = 20` (exposed as a constant near `NODE_COUNT`) replaces the old split top-20/proximity-14 pools with one pool of `LABEL_TOP_K + 1` slots — top-K by `vol24h`, plus the hovered/selected node if it isn't already in that set (given top overlap priority). A thin per-frame leader-line (`leaderLines`, one `LineSegments` buffer) ties each visible label to its node centre in the node's own colour at low opacity; the colour-matched dot and a colour-tinted underline strip are baked into the label canvas itself. **Added a small, self-contained hover/tap-to-select interaction** (a `Raycaster` against `nodeMesh`, throttled to ~90ms in the render loop, plus a tap handler on the existing canvas `pointerdown`) purely to resolve a truncated label's full name in the existing `#node-info` readout (now takes an optional `type`, omitted for hover/select vs. `BUY`/`SELL` for trade events) — this is new interactivity, called out explicitly since the task's own guardrails said "no interaction changes" while also requiring "full name on hover/select" for an interaction that didn't previously exist; it's additive (separate listeners, doesn't touch drag/look/WASD/gyro) and was the most direct way to satisfy the explicit pass condition.
  **Fix 3 (real 3D nodes):** swapped `IcosahedronGeometry(1,1)` for `SphereGeometry(1,24,16)` (720 tri/sphere × ≤220 instances = ~158k triangles in one draw call — trivial budget, no impostor fallback needed; real Quest/mobile fps still unmeasured, see caveat below), retuned the key/fill light rig (hemi 0.55→0.32, directional 0.35→0.45) so MeshStandardMaterial (roughness 0.4, metalness 0) actually shows a light-side/dark-side gradient instead of reading flat under the old weak rig + strong flat emissive. Added a fresnel rim and per-hue emissive/bloom-luminance normalization via `onBeforeCompile` (kept to the material's own shader, not a second mesh, so the node system stays one `InstancedMesh`/one draw call) — hit two real bugs while wiring this up, both fixed and worth remembering: (1) fully *replacing* `#include <emissivemap_fragment>` instead of appending after it caused a `totalEmissiveRadiance` redefinition + `emissiveIntensity` undeclared-identifier shader compile error in this three.js version's chunk assembly — fixed by keeping the stock include and only multiplying afterward (the standard `onBeforeCompile` pattern: augment, don't replace); (2) after that compiled cleanly, every high-volume (typically larger) node still blew out to flat white — traced to reusing the *existing* per-instance `instanceColor`/`vColor` (which already carries the pre-existing flare/volume brightness multiplier, uncapped up to 1.6×) as the emissive tint too, compounding with the new per-hue emissive scale; fixed by adding a dedicated `instanceEmissiveColor` attribute carrying the node's pure hue (unmultiplied), separate from the intensity-boosted diffuse `instanceColor`, so emissive brightness no longer double-counts trade volume. Also hit a stale-GPU-shader-cache red herring mid-debug: editing `onBeforeCompile` and reloading the *same* long-lived Preview browser tab kept showing the old compile error verbatim even after the source was fixed (confirmed via direct `curl` of the served file) — a fresh browser process picked up the fix immediately, so a stuck shader-cache/compiled-program artifact on that tab, not a real bug; worth remembering for future shader-patch debugging in this harness.
  **Verification:** same harness constraint as Phase 1 (Chrome extension refuses `localhost`/`file://`; used the Preview tool's managed browser via `preview_eval`, scripted, not manual). Confirmed per pass condition: (1) truncation/ellipsis exact-matched a real 14-char live ticker, auto-fit canvas dimensions verified to vary correctly with text length, no clipping; (2) hover confirmed end-to-end (dispatched real `PointerEvent`s, checked internal state via a debug hook extended with `labelPool`/`hoveredNodeIndex`/`selectedNodeIndex`/`dragging`/`pointerLocked` getters — hover correctly resolves the hovered node's word, populates `#node-info` with no type prefix, and takes label-pool slot 0); leader-line geometry confirmed populated (`drawRange` > 0) and visually present tying labels to spheres; (3) spheres confirmed showing real per-hue lit gradients + fresnel rim in both `?data=mock` (220 nodes) and `?data=live` (51 real nodes) after the exposure/decoupling fixes, no shader console errors in a fresh browser process; (4) no doubled/overlapping labels observed at rest across multiple screenshots; (5) frame rate NOT measured live — the same backgrounded-tab `requestAnimationFrame`-freeze harness limitation as prior phases made a real rAF-timed FPS loop hang, so perf confidence rests on the triangle-count/single-draw-call budget reasoning above, not a measured number; genuine on-device Quest/mobile fps remains unverified, consistent with every prior phase's status log entry. `?data=mock` regression-checked after the fixes (unchanged node count/badge/behaviour aside from the new hover affordance noted above). Screenshots: `verification/wordmesh-cosmetic-mock-labels-3d.png`, `verification/wordmesh-cosmetic-live-labels-3d.png`. Per the task's explicit "do not act yet" instruction, the `threejs-visual-polish` skill itself was NOT updated this session — canvas label auto-fit+ellipsis, leader-line tethering, and lit-instanced/impostor 3D nodes with per-hue emissive normalization (plus the two onBeforeCompile gotchas above) are noted here as candidates for that skill once confirmed on-device.

- 2026-07-09: Mobile joystick + AR placement fixes (functional only, per `webxr-threejs`; labels/colors/materials untouched). **In-harness logic confirmed for both; real-device confirmation on Android from the user is still outstanding — do not treat either as on-device-verified.**
  **Fix A (Android joystick crooked):** root cause was missing pointer-id scoping on the look-drag system (`dragging`/`lastX`/`lastY`/`yaw`/`pitch`) — the joystick's own move handler already filtered by `e.pointerId`, but the shared look-drag `pointerup`/`pointermove` listeners on `window` reacted to *any* active pointer, so on natural two-thumb mobile play (joystick thumb + a second finger dragging to look) the joystick thumb's own move/lift events corrupted or ended the look-drag, scrambling the yaw the joystick's camera-relative movement is derived from — that's what read as "crooked." Fixed by adding `dragPointerId` and filtering every look-drag listener to it, plus a `pointercancel` handler. Also implemented "recentre stick base at touchstart" (was using the widget's fixed geometric centre via `getBoundingClientRect()`, not the actual touch point, biasing the zero-point by however far the thumb missed dead-centre) — the base/nub graphics now float to the touchdown point via inline `cssText`, docking back to their CSS-default idle position/appearance on release (so idle visuals are byte-identical to before). Vertical fly buttons were already isolated (separate DOM elements, local pointer handling, no shared state) — confirmed unaffected, untouched. Joystick↔tap-select coexistence was already structurally sound (`#joystick-wrap`/`#vert-wrap` are `position:fixed` siblings over the canvas with their own hit area, so a joystick touch never reaches canvas's `pointerdown` at all via normal DOM hit-testing) — verified this holds, didn't need to change the select path.
  **Fix B (AR mesh offset):** `enterXR()` never reset the shared `rig` (position or rotation) on session start, and `renderer.xr.setReferenceSpaceType(...)` was never called (defaults to Three.js's `'local'`, not `'local-floor'`, despite `local-floor` being listed only as an *optional* `requestSession` feature — that alone doesn't make Three.js request it for camera pose). Since `rig.position` accumulates freely while flying in Screen mode and the same rig is reused across every mode, entering AR from wherever Screen-mode flight last left off dropped the viewer beside/outside the mesh instead of inside it — matches "sits off to the left." There's no separate mesh-root transform to translate: `initNodes()` already spawns every node in a sphere centered at world `(0,0,0)`, confirmed by computing the live centroid of 220 alive nodes (`(0.42, -0.51, -0.7)` — negligible offset from true origin, node distances 10.3–58.9 units out). Fixed by adding `renderer.xr.setReferenceSpaceType("local-floor")` once at renderer setup, and `rig.position.set(0,0,0); rig.rotation.set(0,0,0);` in `enterXR()` right before `renderer.xr.setSession(session)` — applies to both VR and AR since they share the one function/rig, and there's no reason a VR user should spawn wherever they last flew to either. Mesh scaling for a "comfortable around-you AR size" was explicitly optional in the task and skipped — the primary bug (viewer outside the mesh entirely) is what's fixed; the mesh's natural ~14-60 unit radius may still feel large for a room-scale walk, a candidate follow-up once the placement itself is confirmed on-device.
  **In-harness verification (what could and couldn't be checked here):** Chrome extension still can't reach `localhost`; used the Preview tool + `preview_eval` as before. `matchMedia("(pointer:coarse)")` could NOT be forced true through this harness (`preview_resize`'s mobile preset only changes viewport dimensions, not touch-capability emulation via CDP), so the joystick's own on-screen UI never activated and its visual/DOM-specific code (the `cssText` recenter, the `#joystick-wrap` pointerdown gate) is verified by code review only, not exercised. What *was* directly exercised via synthesized `PointerEvent`s: the shared pointer-id-scoping mechanism (a "stranger" pointer's moves/lift, simulating the joystick thumb, left a drag-owner pointer's `yaw`/`pitch` completely unchanged and didn't end its drag; the real owner's move still applied correctly, math-verified against the exact `dx * 0.0035` formula) — this is the actual root-cause fix, and it's the same protection pattern the joystick's own (pre-existing, unchanged) handlers already used; camera-relative movement alignment (set an arbitrary yaw, drove `joyVec` directly via a debug setter, measured the resulting `rig.position` delta against the expected camera-forward vector at that yaw — dot product of exactly `1.0`, i.e. perfectly aligned, not approximate); tap-to-select still resolves the correct node post-edit (no regression). For AR: `navigator.xr` exists in this environment but `requestSession('immersive-ar', ...)` has no real device to succeed against, so `enterXR()` itself was never run end-to-end — verified the exact reset statements restore `rig.position`/`rig.rotation` to `(0,0,0)` from an arbitrary simulated Screen-mode fly-away, and separately confirmed `(0,0,0)` is genuinely the mesh's centroid (above). Explicitly NOT verified here and NOT claimed: real touch feel, real multi-touch on a physical screen, and any real AR session (passthrough, tracking, the actual visual "user surrounded by nodes" outcome) — those need the user on a real Android phone.
  **Awaiting user device check (Android, real hardware):** (a) joystick — pushing the stick moves in the pushed direction relative to facing, no drift/skew, move+look multi-touch works simultaneously, vertical buttons unaffected, tapping a node still selects it; (b) AR — session starts with the viewer centered inside the mesh (nodes surrounding them, not jammed to one side), physically walking moves through the cloud. Skill fold-in into `webxr-threejs` (mobile joystick camera-relative + per-touch-id pattern; AR entry rig-reset + local-floor pattern) is deferred until the user confirms both on-device, per the task's explicit instruction — not done this session.

- 2026-07-09: In-scene token UI (survives VR/AR) + full-mode selection. **Screen-mode + code-level verification done; immersive rendering and controller-laser selection are awaiting the user on a real headset — not claimed here.**
  **Step 0 findings:** confirmed `#node-info`, the `MOCK`/`LIVE DATA` badge, the mode switcher, and Fly/Free-look are all DOM (`#hud`), and `#hud.in-xr { display:none }` + `hud.classList.add("in-xr")` on session start hides all of it the instant XR begins — exactly the reported problem. From code, the render loop updates nodes/labels/pulses unconditionally every frame regardless of `renderer.xr.isPresenting` (only the final draw call branches render-vs-composer), so the mesh should keep rendering/updating in a real session — needs the headset to actually confirm. Selection lived in two module-scoped vars (`hoveredNodeIndex`, transient; `selectedNodeIndex`, previously auto-expired after 4s) feeding `updateNodeInfo()`/`#node-info` via one `Raycaster` fed by canvas `pointerdown`/hover.
  **Sparkline probe:** generic query params on `/api/transactions` (`sale_address=`, `token_address=`) are silently ignored and just return the unfiltered global feed — but `/api/tokens/{sale_address}/transactions` is genuinely per-token (confirmed against two different tokens, correct `symbol` echoed in each response; hourly OHLC candles, CORS-open). Added `SuperheroSource.getTokenHistory(saleAddress, hours)`; MockSource has no such method, and the panel checks `typeof source.getTokenHistory === "function"` before ever calling it, so mock mode omits the sparkline area entirely rather than fabricating one.
  **What shipped:** a world-space detail panel (`PlaneGeometry` + DPR-aware `CanvasTexture`, `MeshBasicMaterial` double-sided) that docks ~1.35m in front of the head on open (offset slightly below eye level), then stays world-locked — only its rotation billboards around the vertical (Y) axis each frame to keep facing the user, position doesn't chase the camera. Re-docks fresh on every new selection. Content: full name, price, vol24h, and — live mode only — a real per-token sparkline (never drawn if the fetch fails or returns nothing). A dedicated leader line ties the panel back to its node in the node's own colour, separate from the label system's leader-line buffer (different concern, kept unmixed). `selectedNodeIndex`'s 4s auto-expiry was removed — it's now sticky (closes only on deselect or the node dying), since a persistent detail panel needs real "open until closed" semantics, not a flash; this is the same state variable, not a second one, and it still drives label top-priority and `#node-info` exactly as before. Selection itself is unified: `selectFromRay()` is one function called by mouse/tap (`raycaster.setFromCamera` from screen coords — this already covers handheld AR unchanged, since Three.js keeps `camera` synced to the tracked device pose while presenting, confirmed by reasoning through the renderer's XR camera handling, no separate AR-tap code needed) and by a new VR/AR controller-laser path (`renderer.xr.getController(0)`/`getController(1)`, parented to `rig` to match `camera`'s own parenting, trigger = `buttons[0]`, bound the same on both hands per the webxr-threejs standard so no handedness branch was needed for selection specifically). Selecting empty space (a raycast miss) now explicitly closes the panel and clears `selectedNodeIndex`, and the panel's own per-frame update closes itself if its node dies (Erlang beat). The highlight-on-select reuses the *existing* `flare` mechanic as a sustained floor (`SELECTED_FLARE_LEVEL`) rather than adding new material state — `flare` has always only ever scaled `baseColor` by a multiplier, never touched hue, so "brightness/shine only, never a colour change" was true by construction; verified directly (`baseHue === instHue` exactly, to 4 decimal places, while `flare === SELECTED_FLARE_LEVEL`). An in-scene status chip (parented directly to `camera`, not `rig`, so it's rigidly stuck in the view corner in every mode) mirrors the DOM badge text/colour, built with the same measure-then-size canvas approach as the labels — sized/positioned conservatively (small, near-centre-low) after an initial version at a wider offset clipped off-screen at narrower aspect ratios.
  **Bugs found and fixed along the way:** (1) node entries never carried `saleAddress` from the snapshot token into `nodeData` (only `price`/`volume24h`/`marketCap` were copied) — silently broke the sparkline fetch (`n.saleAddress` was `undefined`) until added; (2) the panel plane initially rendered mirror-flipped text — `PlaneGeometry`'s front face is +Z, and I assumed `Object3D.lookAt()` points the local -Z axis at the target (matching camera/WebXR `targetRaySpace` convention) and added a corrective `rotateY(Math.PI)` — empirically this was backwards for a generic (non-camera) `Object3D`, confirmed by comparing `(0,0,-1).transformDirection(matrixWorld)` against a manually-computed direction after `lookAt()`; removing the extra rotation fixed it. Worth remembering: `lookAt()`'s facing convention isn't uniform across all three.js object types the way the WebXR spec's -Z `targetRaySpace` convention is — don't assume they match without checking. (3) `renderer.xr.getController(index)` objects have `matrixAutoUpdate: false` by design (pose is meant to be driven exclusively by the WebXRManager during a real session) — manually setting `.position` on them for testing silently does nothing to `.matrixWorld`; this is correct production behaviour (confirms I'm using the standard three.js pattern) but means controller-ray selection couldn't be exercised through this harness at all, only the ray-extraction math in isolation (see below).
  **In-harness verification:** same constraints as prior sessions (Chrome extension can't reach `localhost`; used the Preview tool + `preview_eval`, restarted the browser process after edits per the stale-GPU-cache lesson from the cosmetic pass). Directly exercised via synthesized `PointerEvent`s + state inspection: opening a node (correct node selected, panel becomes visible, leader-line `drawRange` populated, panel text legible not mirrored); opening a second node auto-closes/replaces the first (`panelNodeIndex === selectedNodeIndex` stays a single value); tapping empty space closes the panel (`selectedNodeIndex`/`panelNodeIndex` both `-1`, `panelMesh.visible === false`); the sustained highlight is hue-exact (above); live-mode sparkline fetch resolves real per-token OHLC data (confirmed 49 points for two different tokens) and renders as a real line, mock-mode panel omits that area entirely; the status chip renders and stays within frame after the resize fix; drag-look and tap-select still coexist correctly on the same `pointerdown` (fresh-reload test: yaw changes correctly, `dragging` cleans up correctly — an earlier same-page test showed stale `dragging:true` from a prior test's mismatched pointer id, not a real bug, resolved on reload). Controller-laser selection: could not exercise `pollVRControllers`'s trigger-press branch through a real controller (see bug #3 above), but isolated and verified the exact ray-extraction formula it uses (`origin.setFromMatrixPosition` + `(0,0,-1).transformDirection`) against a matrix explicitly constructed to the WebXR `targetRaySpace` -Z convention — correctly resolved the intended target every time. `navigator.xr` exists in this environment but no real session can be entered, so `enterXR()`/an actual trigger press were never run end-to-end. Screenshots: `verification/wordmesh-inscene-panel-live-sparkline.png`, `verification/wordmesh-inscene-panel-mock-nosparkline.png`.
  **Not verified here, awaiting the user on a real headset:** the panel actually rendering and staying legible/well-placed *inside* a live VR or AR session; controller-laser trigger selection against a real controller; the DOM badge correctly disappearing while the in-scene chip remains visible in-headset (code says it should — `#hud.in-xr` — but not watched happen). Skill fold-in into `webxr-threejs` (in-scene docked-billboard panel pattern; unified mouse/controller-laser/AR-tap raycast selection) deferred until the user confirms on-device, per the task's explicit instruction.

- 2026-07-09: Live data is now the default (Batch 1 of 2 — a search/VR-keyboard/teleport batch follows, so the DataSource seam was kept clean for it). `?data=` is no longer required for the real mesh; mock only exists behind a hidden `?data=mock` dev flag, never surfaced in any UI, never the default.
  **Mock removed from the default path:** `dataMode` now resolves to `"live"` unless the URL explicitly says `?data=mock` (previously the reverse). The blocking top-level `await liveSource.getSnapshot()` that used to gate the *entire* module — delaying renderer/scene/starfield setup on live loads, and silently falling back to a 220-node `MockSource` population on any failure — is gone. `initNodes()` was split: `buildNodesFromSnapshot(snap)` does the actual per-word node construction (unchanged logic, just factored out and now also resolves `words` from the real token list for live mode), and a new `connectDataSource()` drives it — mock calls it once (synchronous, can't meaningfully fail); live retries with linear backoff (5s, 10s, 15s… capped at 30s) until a snapshot succeeds, and *never* falls back to fabricated data. Scene/renderer/starfield now render immediately regardless of connection state, since nothing blocks on the fetch anymore.
  **Honest connection states:** the DOM badge and a new repaintable in-scene status chip (previously painted once and fixed for the session) both now drive off one shared `CONNECTION_STATES` config (`mock` / `connecting` / `offline` / `live`, each with its own text + colour), through a single `setConnectionState(state)` call so the two can never drift out of sync — `CONNECTING…` (neutral blue) on first load, `LIVE DATA` (green, unchanged) once a snapshot lands, `OFFLINE — RETRYING…` (red) on any failure, retried automatically and indefinitely (not a one-shot give-up). A real bug surfaced building this: a freshly-created `InstancedMesh` defaults every unset instance to an identity matrix (visible, at the origin) — without explicitly hiding all `NODE_COUNT` instances up front (added at mesh-creation time, before any data exists), the "connecting"/"offline" window would have shown a phantom cluster of unit spheres at world origin instead of an honest empty mesh. Scoping note, stated plainly rather than silently: this retry loop covers the *initial* connection only (matches the task's literal framing — "killing the network shows the offline state," read as a cold-start scenario) — there's no ongoing post-connection health monitor, so if the network drops *after* a successful load, the badge stays on `LIVE DATA` and the mesh just goes stale rather than flipping to offline (the WS layer has its own independent `connect_error` logging from Phase 1, unchanged). Flagging this now rather than have it discovered later; a periodic post-connection health check is a candidate for the next batch if wanted.
  **In-harness verification:** same constraints as before (Chrome extension can't reach `localhost`; used the Preview tool + `preview_eval`). Confirmed: default load (no query param) reaches `LIVE DATA` with real ~51 tokens on first successful fetch, both badge and chip green, no mock code path executed; `?data=mock` still produces the unchanged 220-node mock mesh with the orange `MOCK DATA` badge; offline retry verified by monkey-patching `source.getSnapshot` to reject and manually driving `connectDataSource()` — badge/chip correctly flip to `OFFLINE — RETRYING…`, existing (already-loaded) real nodes are *not* cleared/blanked while retrying, and restoring `getSnapshot` lets the next scheduled retry reconnect to `LIVE DATA` automatically; selection/panel (built last session) re-confirmed working unchanged against the new live-default flow (correct node selected, panel opens with real price/vol24h + sparkline, leader line, no regression) — the first attempt at this specific check hit the known backgrounded-tab harness quirk (`visibilityState: hidden` → `window.innerWidth/Height: 0` → `camera.aspect: NaN`, breaking only the *test's* screen-projection math, not the app), resolved by restarting the preview browser and forcing a render via `preview_screenshot` before rechecking — a harness artifact, not a regression, consistent with the standing hidden-tab testing note. Screenshot: `verification/wordmesh-live-default-with-panel.png` (default load, no param, real tokens, panel + sparkline + green LIVE badge all together).
  **Awaiting the user (later, on device):** confirm the live mesh loads by default on a real headset/phone with no query param.

- 2026-07-09: Search + in-scene laser keyboard + teleport (Batch 2 of 2). Runs on top of Batch 1 (real-data-default) and the in-scene UI batch (80611d4) — deliberately reused their panel-docking, raycast-selection, and node-selection machinery rather than building a second path. **Screen-mode + code-level verification done; controller-laser typing, real VR/AR search+teleport, in-headset arrival, and the physical left-X comfort toggle are awaiting the user on real hardware — not claimed here.**
  **Shared-code refactor (behaviour-preserving, done first so the new UI could reuse it):** factored the existing detail-panel's docking/billboard logic into `dockInFrontOfHead(obj, dist, drop)` and `billboardYAxis(obj)` (head-anchored placement on open, world-locked position, Y-axis-only billboard rotation per frame), and factored node-selection into `selectNode(n)` (`selectedNodeIndex` + `updateNodeInfo()` + `openPanelFor()`) out of the existing `selectFromRay()`. Both the detail panel and the new search panel now call the same docking helpers; both direct raycast clicks and teleport-arrival now call the same `selectNode()`. Verified behaviour-preserving via `node --check` and a full re-run of the existing selection/panel checks before building anything new on top.
  **What shipped, part 1 — search button:** a persistent camera-attached magnifier icon (`searchButtonMesh`, hand-painted canvas glyph, opposite corner from the status chip, `depthTest:false` so it's always on top) toggles a `searchGroup` (panel + keyboard + key labels, grouped so one docking transform moves all of it). Opens head-anchored via `dockInFrontOfHead`, billboards every frame while open, mutually exclusive with the detail panel (opening search force-closes any open node panel and vice versa isn't needed since search closes itself on selection).
  **Part 2 — search panel + in-scene laser keyboard:** the panel is the same canvas-texture-on-plane pattern as the detail panel (`searchPanelCanvas`, DPR-aware). The keyboard is one `InstancedMesh` (`keyboardMesh`, low-poly boxes, `KEY_SIZE=0.042`) covering QWERTY + backspace/clear/close, each instance's world position set once at layout time from a shared `KEYBOARD_LAYOUT` array — the exact same InstancedMesh-plus-`instanceId`-lookup pattern already proven for node selection, so keyboard keys are raycast targets for free through the existing raycaster. A small static label sprite sits in front of each key box (no per-key press/hover flash — a deliberate scope cut; the query text updating live is the primary feedback). Real hardware-keyboard input was not added (explicitly marked optional in the task).
  **Part 3 — live match list:** `computeSearchMatches(query)` filters the currently-loaded `nodeData` (case-insensitive name/symbol), prefix matches ranked before contains-matches, top 6, re-run on every keypress. Zero matches paints an explicit "no token found" — never a fake/placeholder result. Match rows are hit-tested via UV-region lookup on the single search-panel plane (`matchRowAtUV`, with the WebGL `v=1-uv.y` flip applied explicitly) rather than one mesh per row — a deliberate lighter-weight choice for a max-6-row dynamic list, called out here as a complexity tradeoff rather than left implicit.
  **Part 4 — teleport + comfort fade:** selecting a match closes search and either (default, comfort fade off) starts a visible `teleportAnim` — ease-in-out cubic dolly of `rig.position` over 1.0s to a framing point ~8 units out from the target node along the existing approach vector, calling the shared `selectNode()` on arrival (same highlight-brightness-only + detail-panel path as a direct click, by construction, not by duplicated logic) — or (opt-in via a new "Comfort fade" pill button bound to the physical left controller's X button, `buttons[4]`, `localStorage`-persisted as `wm_comfort_fade`, off by default) wraps the same jump in a `fadeAnim`: a camera-attached fullscreen black plane (in-scene, not DOM, since DOM is invisible in a real XR session) fades to opaque over 0.2s, snaps `rig.position` + calls `selectNode()` while covered, fades back over 0.2s.
  **In-harness verification:** same constraints as every prior batch (Chrome extension can't reach `localhost`; Preview tool + `preview_eval`). Directly exercised via synthesized `PointerEvent`s dispatched on the real canvas (not just calling functions directly) for the two raycast-dependent paths: the search button (`pointerdown`/`pointerup` at its screen position correctly set `searchOpen: true`, `searchGroup.visible: true`) and a keyboard key (`pointerdown` on the "E" key's screen position correctly typed "E" into `searchQuery` via the real `instanceId → KEYBOARD_LAYOUT → pressKey` path, not a shortcut) — both prove the InstancedMesh raycast-hit-testing pattern works for the new meshes exactly as it does for node selection. Live filtering confirmed against real loaded tokens (`"E"` → 6 matches including `ETHEREUM`/`ETH`; `"ETH"` → narrows to `ETH`, `ETHEREUM`). Zero-match state confirmed both in data (`searchMatches.length === 0`) and rendered (`"no token found"` visible in a screenshot). `selectSearchResult()` confirmed to set a correctly-shaped `teleportAnim` (`from`/`to`/`node` all correct, `to` at the expected ~8-unit framing distance) when comfort fade is off, and a correctly-shaped `fadeAnim` (`phase: "out"`, correct node/target) when comfort fade is on — the branch is real, not assumed. Hit the same backgrounded-tab `requestAnimationFrame`-freeze harness limitation noted in every prior batch's log (`document.visibilityState: "hidden"` here specifically, confirmed via direct inspection) — the animations themselves (`teleportAnim`/`fadeAnim` ticking frame-to-frame, the camera actually dollying) could not be watched play out live in this harness, since `renderer.setAnimationLoop`'s callback is gated by the browser's real rAF scheduler and a screenshot's forced-render doesn't advance it. Worked around by verifying the two things that actually matter and are harness-independent: (a) the animation *state* set on selection is correct (above), and (b) the arrival *behaviour* — `selectNode()` being called and correctly opening the panel/highlight for the target node — is the exact same function already exercised by direct clicks in the prior batch, re-confirmed directly (`selectedNodeIndex`/`panelNodeIndex` both landed on the target's index, `panelMesh.visible: true`, panel painted the correct name/price/vol24h) via `window.__wm.selectNode`, newly exposed on the debug hook for this. This is a testing-harness limitation identical in kind to the one logged for every previous phase (Phase 0 rAF-freeze note, cosmetic-pass fps note) — not new, not an app bug. Comfort-fade toggle confirmed via a real DOM click on `#btn-comfort-fade`: `comfortFadeEnabled` flips, button text/class update (`"Comfort fade: ON"` / `.pill-btn.on`), `localStorage.wm_comfort_fade` persists `"1"`, and toggling back off restores the default state and the visible-dolly (`teleportAnim`) code path. No console errors observed at any point across the full sequence (load, search open/type/clear, both match-found and no-match states, both teleport branches, comfort toggle).
  **Not verified here, awaiting the user on real hardware:** typing on the in-scene keyboard via an actual VR controller laser + trigger (the controller-ray extraction math itself was isolated-verified in the prior batch and reused unchanged here, but a real controller object was never driven end-to-end, same `matrixAutoUpdate:false` limitation as before); search + teleport actually working inside a live VR or AR session; the arrival highlight/panel appearing correctly in-headset; the physical left-X comfort-fade toggle on a real controller (the button-index binding and the resulting state change were verified in code/logic, not against real controller input); the teleport dolly and comfort-fade actually animating smoothly over real frames (blocked by the harness's rAF-freeze on a backgrounded tab, not something code review can fully stand in for). Skill fold-in into `webxr-threejs` (search-panel/keyboard/teleport patterns) deliberately NOT done this session, per the task's explicit "do not act on this yet" instruction — deferred until the user confirms on-device.

- 2026-07-10: Live-mesh density/framing regression fix (cosmetic only — spawn radius + initial camera distance; no data, selection, locomotion, or mode-switcher code touched). The 14–60 spawn shell and the hardcoded `rig.position.z = 24` were both tuned for `MockSource`'s 220 nodes; live mode reuses the same shell with far fewer real tokens, so the mesh read as a sparse scatter viewed from too far out. Fix: `spawnDensityScale = max(0.5, cbrt(min(1, words.length / NODE_COUNT)))` shrinks the shell (cube-root, since node count scales with volume not radius), applied in `buildNodesFromSnapshot()` and reused by `reviveSlot()` so Erlang respawns land in the same shell; and a one-shot `liveCameraFramed` block sets live's initial `rig.position.z` from the snapshot's *actual* bounding radius (`clamp(avgR*0.65, 8, min(maxR-3, 40))`) instead of assuming mock's density. Mock is untouched: at 220 nodes `spawnDensityScale` is exactly 1.0 (shell stays 14–60) and the camera block is gated on `dataMode === "live"`.
  **Verified here (arithmetic + static, not pixels):** inline module parses clean; `rig` is constructed (line ~452) long before the framing block runs; the code comment's "`buildNodesFromSnapshot` is only ever called once for live" holds — `connectDataSource()`'s retry loop only re-enters when `getSnapshot()` throws *before* the build, and returns on success. Monte-Carlo of the shipped formulas (200k samples, `|pos|` accounting for the 0.6 y-compression) confirms the intended effect: node density per unit shell volume was 1.105 (at 100 tokens) and 0.564 (at 51) against mock's 2.432 — i.e. 2.2×–4.3× too sparse — and lands at exactly 2.432 for both after the fix, with the auto-framed camera placing ~27% of nodes nearer than the rig (mock's hardcoded framing gives 31%), so the viewer sits amid the cloud rather than outside it. The `0.5` floor engages below ~28 tokens, deliberately trading exact density parity for not collapsing a tiny universe into a clump. **Not verified:** any actual rendered frame — the preview server for this repo was held on port 8791 by a concurrent session, so no screenshot/`preview_eval` pass ran this session; visual confirmation is the user's on-device test.
  **~~Stale premise worth knowing:~~ CORRECTED 2026-07-10 (see the entry below):** this entry originally claimed the token universe had grown past 100 and was being truncated by `limit=100`. That was wrong — it came from an ad-hoc `GET /api/tokens?limit=100` that omitted the app's own `order_by=trending_score`. The app's actual query returns **51** tokens and page 2 is empty, so `~51` was right all along. The real (and separate) finding is recorded below: `order_by=trending_score` is itself the filter that holds the mesh to 51.

- 2026-07-10: **Live mode rendered no spheres at all — root cause was frustum culling, not density.** User reported the live mesh on iPhone as a near-empty void: thin edges and a few labels, no node geometry. Reproduced headlessly and proved the cause: three.js caches an `InstancedMesh`'s bounding sphere the first time it frustum-checks it and *never* recomputes it as instance matrices change. Batch 1 (`712d921`) parks all `NODE_COUNT` instances at `(0,-9999,0)` scale `0.0001` up front (to avoid a phantom origin cluster during "connecting"), so in live mode — where nodes are built *after* an async fetch — the first render baked `boundingSphere = {center:(0,-9999,0), radius:0.0001}`, `intersectsFrustum: false`, and the entire node mesh was culled forever (`renderer.info.render: 1 call, 1 triangle`). Mock never hit it because its nodes are built synchronously before the first frame. The same trap silently hit `pulseMesh` (parked until a trade fires → live pulses never drew) and `farPoints` (its position buffer is rewritten per frame and no node starts far in live → LOD dots would never draw). Fix: `frustumCulled = false` on all three. Not `computeBoundingSphere()` — node positions move at runtime (`reviveSlot`), so any one-shot recompute goes stale again; these are three always-on-screen draw calls and culling them buys nothing. **Note the earlier `e1c7e4b` "sparse mesh" diagnosis was treating a symptom of this bug — the mesh was never sparse, it was invisible.** `e1c7e4b`'s density/framing work is kept (it does give live mock-parity density: 2.432 nodes per 1e4 shell-volume either way) but it was never the reason nothing showed.
  **Second, smaller fix, caused by `e1c7e4b` and only visible once the spheres came back:** auto-framing the camera from the dataset's bounding radius parked the rig 3.41 units from a node centre, so one sphere filled the upper half of the viewport. Added a `CAM_CLEARANCE = 8` back-off loop (bounded, capped at `maxR + CAM_CLEARANCE`) that pushes `rig.position.z` out until the nearest node centre clears it. Largest node now reads at 20.4% of viewport width (mock's "good" reference: ~25%); nearest node centre sits at exactly 8.00 units.
  **Verified here, with real pixels:** the repo's `.claude/launch.json` preview server is unusable (the harness caches the launch config — it ignored a renamed config on a different port — and the sandboxed server process can't read `~/Documents`), so verification ran against headless Chrome via CDP over a scratchpad copy served on `127.0.0.1:8899`. Before/after screenshots at 390×844 @2x (iPhone-ish), live mode, default URL with no query param: `verification/wordmesh-live-culled-before.png` (reproduces the user's void exactly — edges + 2 labels, zero spheres) and `verification/wordmesh-live-spheres-restored.png` (51 real tokens as lit, size-varied, emissive spheres). `verification/wordmesh-live-panel-restored.png` shows live selection working end-to-end — `selectNode()` → HUD tooltip `AETERNITY BUY · price 0.0006 AE · vol24h 20957` + in-scene panel with 48h sparkline — and a synthetic `onDataEvent` buy correctly promotes an active pulse (0 → 1) now that `pulseMesh` isn't culled. `verification/wordmesh-mock-unaffected-cullfix.png` confirms `?data=mock` is untouched: 220 nodes, `MOCK DATA` badge, panel + tooltip identical to before. Edge model deliberately not touched. **Not verified:** on-device iPhone/Quest rendering (harness is desktop headless Chrome with SwiftShader); real VR/AR sessions.
  **Real finding about the token universe (replaces the incorrect note in the entry above):** `GET /api/tokens?limit=100` (no ordering) returns **100** tokens, but the app's own `order_by=trending_score&order_direction=DESC&limit=100&page=1` returns only **51**, and page 2 is empty. So the mesh shows 51 because ordering by `trending_score` drops every token without a trending-score row — not because of the `limit=100` cap and not because the universe is small. Raising coverage means changing the ordering/filter, which is a data-behaviour change; left alone deliberately, flagged for the co-mention edge prompt.

- 2026-07-10: Edges vanishing on camera turn (same stale-bounds class as `627b7dd`), bigger mobile labels, HUD cleanup. Device feedback on `627b7dd`: spheres confirmed fixed, but "when u turn right — connection lines disappear", labels "still too small", and two unwanted HUD elements.
  **Edges (and label leader lines) — the same bug one layer down.** `edgeGeo` starts as `drawRange(0,0)` over an all-zero position buffer, so three's first frustum test cached `boundingSphere = {center:(0,0,0), radius:0}` and never refreshed it as `writeEdgeBuffer()` rewrote positions. A zero-radius sphere at the origin survives only while the origin is inside the frustum — turn the camera and the entire edge batch is culled. Measured against deployed `627b7dd` with a yaw sweep: `edgeBsR: 0` throughout, `edgesPassFrustum` flips `true → false` at −29° while **11 nodes are still on screen** — exactly the user's report. `leaderGeo` (label tethers) and `panelLeaderGeo` had it too. Fixed with a `recomputeLineBounds(geo, positions, vertCount)` helper called wherever each buffer's draw range is rewritten. Deliberately *not* `BufferGeometry.computeBoundingSphere()`: that walks the whole backing array including the unused zeroed tail, dragging the centre to the origin and inflating the radius. Deliberately not `frustumCulled = false` either (unlike the InstancedMesh fix, where node positions move at runtime and the meshes are always on screen) — these buffers are rewritten in one place each, so correct bounds are cheap and keep culling meaningful. After: `edgeBsR: 28.9`, `edgesPassFrustum: true` at every yaw from 0° to −90°.
  **Full dynamic-geometry audit** (the task asked for all of them, not just edges): `nodeMesh`, `pulseMesh`, `farPoints` already handled in `627b7dd`; `edgeLines`, `leaderLines`, `panelLeaderLine` fixed here. `panelLeaderLine` still reads as "culled on turn" when `drawRange.count === 0` — correct, there is nothing to draw; it gets real bounds (`r: 7.58`) the moment a panel opens. Starfield/keyboard/panel/sprites are either static or legitimately leave the frustum.
  **Labels.** New `labelSizeBoost`, applied at render time to `distMul` (so it feeds both the sprite scale *and* the overlap hit-boxes from one place, and re-evaluates on resize/rotation instead of being baked into each label canvas): 1.7× portrait, 1.4× narrow landscape, 1.15× desktop. Median on-screen label height at iPhone-ish 390×844: **15.8px → 26.8px**.
  **HUD.** The top-left `LIVE DATA` badge and the in-view `LIVE DATA` chip are both suppressed **only when `connectionState === "live"`** — that state is the expected, uninteresting one. `CONNECTING…`, `OFFLINE — RETRYING…` and the hidden `MOCK DATA` dev flag still surface, so a failed load can never silently read as an empty universe (verified: `?data=mock` still shows its badge). The in-scene chip now exists solely for immersive sessions, where `#hud.in-xr` hides the DOM badge — `applyStatusVisibility()` is called on `sessionstart`/`sessionend`.
  **The bottom "clipped IG… pill" was not text and not a badge — it was the search button.** `searchButtonMesh` was pinned at a hardcoded `x = 0.24`; its right edge lands at 0.2675 while a portrait phone's viewport half-width at that plane is only ~0.198, so the magnifier hung off the screen edge and read as cut-off glyphs. Now derived from the actual frustum (`positionSearchButton()`, re-run on resize): `x = 0.1589`, right edge `0.1864` ≤ `0.1984`. **Deliberately fixed rather than removed** — the task said "remove the clipped pill", but it is Batch 2's only entry point to search, which the user has not tested yet; deleting it would have silently removed a feature. Flagged rather than assumed.
  **Verified with rendered pixels** (headless Chrome + CDP at 390×844 @2x, live default URL; repo preview server still unusable — cached launch config + sandboxed process can't read `~/Documents`): `verification/wordmesh-edges-survive-turn.png` (turned right ~20°, edges present, larger labels, no badge/chip, search button fully on-screen), `verification/wordmesh-hud-cleaned-forward.png`, `verification/wordmesh-mock-unaffected-hudfix.png` (220 nodes, MOCK DATA badge, panel + edges unchanged). Edge *model* untouched, per instruction — only its bounding volume. **Not verified:** on-device iPhone/Quest; real VR/AR chip behaviour. Note first-load framing varies run to run because node positions are random — an unlucky layout can still put one large sphere in a corner (clearance is measured to node *centres*, and a max-radius node at exactly 8 units is ~34% of viewport width).

- 2026-07-10: Tap-to-select restored, near-node labels, search button moved off the fly controls. Device feedback on `8f770a0`: "press on node and the table with data opens — why that's no longer active?", "sometimes the word doesn't come above the node even when u come close", plus the search button sitting on top of the up-arrow.
  **Tap-to-select — a third face of the same stale-bounds bug, and one `frustumCulled = false` did NOT fix.** `InstancedMesh.raycast()` broad-phase rejects the ray against `this.boundingSphere` *before* testing any instance. `627b7dd` stopped the renderer consulting that sphere (`frustumCulled = false`) but left the degenerate parked value `{center:(0,-9999,0), radius:0.0001}` in place, so **picking** kept failing silently: measured `raycaster.intersectObject(nodeMesh).length === 0` and a synthesized `pointerdown`/`pointerup` on a node's exact pixel left `panelNodeIndex: -1`. A single `computeBoundingSphere()` flipped it to 1 hit and the panel opened. Nothing to do with touch events, the overlap hit-boxes, or the badge/chip visibility gating — all three were ruled out by measurement, not inspection. Fix: `refreshNodeBounds()` (compute + 15% pad for per-frame flare growth) called after every build and every `reviveSlot()`, and unused/hidden instances now park at the **origin** rather than `y=-9999` so the sphere stays tight (r≈40 instead of 5017). Verified: tap opens the correct node's panel (`correctNode: true`), tapping empty space closes it, and mock behaves identically.
  **Labels missing on near/large nodes — two independent causes, both measured.** (1) `updateVisibleLabels()` ranked *purely* by `volume24h`, and the pool is only 21 slots: e.g. `BATTLESTAR-GALACTICA` at 9.1 units and 33px radius had `volRank: 44` and simply got no slot, while distant high-volume nodes kept theirs. Added `NEAR_LABEL_SLOTS = 6` reserved for the closest nodes (`chosen` is also the overlap-suppression priority order, so near labels now win collisions). (2) Two geometry bugs bit only at close range: the fixed `smoothstep01(2, 6, dist)` near-fade meant a big node (surface radius ≈1.07) put the camera *inside* the fade band — a node at 3.4 units rendered its label at 0.26 opacity — and the fixed label offset `1.25*scale + 0.5` subtended 41° at that distance, past the 31° half-fov, so the word sat outside the frustum. Both are now relative to the node's own radius/distance.
  **A defect my own fix exposed:** once near nodes got labels, `distMul`'s `0.85` floor became visible. On-screen label height is proportional to `distMul/dist`, so the `dist/24` term is what holds it constant as you move; the floor kicked in below ~20 units and killed that compensation, blowing a close-up word up to ~170px tall and clipping it off both screen edges (caught in a render, not in the numbers — the first screenshot after the ranking fix showed "WORD-CRAF"). Floor lowered to `0.10`. Label height is now a uniform **26.6–27.6px** across the whole mesh, near and far.
  **Search button** now reserves the bottom-right 190px band (`#vert-wrap` is `right:20px bottom:20px`, 148px tall). Verified in screen space with the mobile controls forced visible: button rect `(416,498)-(485,567)`, `vert-wrap` `(416,589)-(480,737)`, joystick `(20,627)-(130,737)` — no intersection with either, and fully on-screen.
  **Verified with rendered pixels** (headless Chrome + CDP, 390×844 @2x, live default URL): synthesized real `PointerEvent`s rather than calling `selectNode()` directly, so the whole canvas → raycaster → panel path is exercised. `verification/wordmesh-near-node-label-and-tap.png` (a node filling the viewport at 210px radius, its word correctly sized and anchored above it, search button clear of the arrows), `verification/wordmesh-tap-select-restored-forward.png`, `verification/wordmesh-mock-unaffected-tapfix.png`. **Not verified:** on-device iPhone/Quest; VR controller-laser picking (uses the same `nodeMesh` broad-phase, so it was equally broken in live and should now work — but untested on hardware).
  **Standing lesson for this file:** three.js caches bounding volumes for *both* frustum culling and raycasting, and they are consulted independently. `frustumCulled = false` silences only the first. Any time instance matrices or buffer positions change after the first frame, both paths need thinking about — this bug has now surfaced three times (`627b7dd` rendering, `8f770a0` edge lines, here picking).

- 2026-07-10: Node identity made single-source-of-truth; label occlusion; micro-label fix. Device feedback on `945dabd`: "woodcraft shows instead of Pokémon, which is duplicated… cards and words and spheres often don't match", plus a garbled cluster of micro-labels beside a large sphere.
  **The identity bug: `byWord` collapsed tokens that share a symbol.** `buildNodesFromSnapshot()` did a positional→word→token round-trip: `words[i]` then `byWord.get(word)`. Token symbols are **not unique on-chain** — anyone can mint a second `POKEMON` — and `new Map()` keeps only the last entry per key. So two spheres both rendered as "POKEMON" while *both* carried the second token's price/marketCap/saleAddress, and the first token's data was unreachable. Reproduced deterministically with a three-token fixture (two `POKEMON`, one `WORD-CRAFT`): old mapping gives node 0 the wrong sale address, new mapping gives each node its own. **Node i is now token i**; never look a token back up by its word. Duplicated symbols are kept as distinct nodes (they are distinct tokens) and get a `displayName` of `WORD·<addr4>` so identical words never appear twice with different data behind them; a `console.warn` fires when the snapshot contains any. Trade/replay events now resolve through `nodeBySale` (sale_address is the only unique id), falling back to word only when an event carries no address.
  **`WORD-CRAFT` and `WORDCRAFT` are genuinely two different tokens** — `ct_wS3iFu3…` at 0.007514 AE and `ct_rBrXTnt…` at 0.001242 AE. That matches the user's S2 (0.0075) and S3 (0.0012) exactly. `wordFromToken` keeps hyphens (`[^A-Z0-9-]`), so they already displayed distinctly; not a normalization bug.
  **Labels appearing on the wrong sphere was a *visual* problem, not an identity one.** Measured with a race-free probe (read label binding + card at one instant): every tap already opened the card of the node actually under the pixel, and no two sprites shared a binding. What deceives the eye is that a label is anchored *above* its own node, so when that node sits behind a nearer sphere the word clears the blocker's silhouette and reads as belonging to it — tap the sphere, get a different token. Added `labelOccluded()`: analytic ray-vs-sphere from the eye against the node list (~21 labels × ~51 nodes/frame), **not** a raycast against the 220-instance mesh (~150k triangle tests per label). Occluded labels are hidden, and an occluded node no longer consumes one of the six reserved near slots. This also explains and fixes S4's stacked micro-labels: they belonged to nodes hidden behind the giant sphere.
  **Two real bugs found while verifying, both invisible in the numbers alone.** (1) The label loop measured distance and line-of-sight from `rig.position`, but the camera sits `EYE_HEIGHT` (1.6) above it — occluded labels slipped through near a large sphere until the test used `camera.getWorldPosition()`. LOD still uses the rig, whose threshold is tuned against it. (2) `distMul`'s upper cap of `2.2` was the mirror image of the `0.85` floor fixed in `945dabd`: past ~53 units it stopped the `dist/24` compensation and shrank distant words to ~12px — the unreadable micro-text. Cap raised to `4.5`; on-screen label height is now a flat **26.8px** at every distance from 2.8 to 47 units.
  **Harness lesson, cost ~20 minutes:** three probes reported `bound: 0` labels and an empty tap list, which looked exactly like a catastrophic regression. The tab was `visibilityState: "hidden"` — Chrome froze `requestAnimationFrame`, `renderer.info.render.frame` stayed at 0, and the render loop never ran. This is the standing hidden-tab gotcha. The CDP driver now sends `Page.bringToFront` + `Emulation.setFocusEmulationEnabled` before navigating, and captures `Runtime.exceptionThrown` (uncaught rAF errors were previously invisible). An earlier "label word ≠ bound node" reading was also a **probe race**, not an app bug — the pool rebinds every 350ms, and the probe read the sprite after the tap but the node before it.
  **Verified with rendered pixels** (headless Chrome + CDP, 390×844 @2x): live and mock both show 0 label/node text mismatches, 0 duplicate label texts, 0 duplicate bindings, and every synthesized tap opens the card of the node it aimed at with a matching price (`ETHEREUM→ETHEREUM`, `WORD-CRAFT→WORD-CRAFT`, mock `PAPER→PAPER`). Standing 2.8 units from the largest sphere: 15 labels shown, **0 shown-but-occluded**, 0 pairs within 30px, all 26.8px. Screenshots: `verification/wordmesh-label-occlusion-nearsphere.png`, `wordmesh-identity-live.png`, `wordmesh-identity-mock.png`. **Not verified:** on-device iPhone/Quest; a real duplicate-symbol snapshot (today's API returns 51 unique symbols, so the duplicate path is proven by fixture, not by live data).

- 2026-07-10: Selection readout corruption fixed — measured first, per this file's standing lesson. Device report: "clicking the same node several times changes/corrupts the panel data." Reproduced in headless live mode (5× same-node taps, A→B→A races, synthetic trade events with the async sparkline fetch actually running) and measured each hypothesis rather than guessing:
  - **CONFIRMED (primary): two readout channels, and live trade events wrote the selection one.** `updateNodeInfo()` painted a top-left DOM `#node-info` box and was called by selection, hover, AND `flareNode()` — the live-trade handler — with a `BUY/SELL` prefix. Measured: panel correctly showing EMOTER, one synthetic buy on YOUR-MOM rewrote the DOM box to "YOUR-MOM BUY · price… vol…" while the in-scene card still showed EMOTER. The two readouts desynced and the visible top-left one flipped to whatever token just traded — read as "the data changes on its own." Fix: **removed the DOM readout entirely** (element, CSS, `updateNodeInfo`, `lastInfoNode`/`infoTimeout`, and all three call sites); the in-scene panel is now the single authoritative selection readout in every mode. Trade events still flare the node in 3D + pulse, but write no readout (their own activity feed is a later phase).
  - **NOT firing: stale sparkline clobber (the prime *suspected* cause).** The existing `openPanelFor` guard (`panelNodeIndex === n.index` inside the `.then`) already dropped late responses: measured a token-B fetch resolving 1.3s late after re-selecting A — correctly discarded, final `panelHistory` owned by A. Still **hardened** it per spec to key on `saleAddress` (the unique token id) instead of node index, and to repaint from the currently-selected node object; symmetric drop of any response whose token isn't the open one.
  - **NOT present: repeated-tap churn / toggle desync / raycast drift.** 5× rapid same-node taps held `panelNodeIndex` and word/price/sparkline perfectly stable (`openPanelFor`'s `isNewNode` short-circuits the refetch+redock on same-node re-tap — already idempotent).
  - **NOT present: duplicate co-mention labels.** `api/posts`/co-mention doesn't exist in this build yet; the "LOVE ×5" the user saw earlier was the duplicate-label bug already fixed in `f0881aa`/`f89dcbd`. Re-confirmed 0 duplicate label bindings/texts across 8 rapid selections.
  **Verified with pixels** (headless Chrome+CDP, 390×844 @2x): live — DOM readout gone, same-node 5× stable, trade-on-other-token leaves panel untouched, A→B→A race ends on the selected token with its own history, empty-space closes, other-node switches, 0 duplicate labels; screenshot shows the single in-scene card (`AI · 0.0032 AE · vol24h 0` + 48h sparkline) with nothing top-left. Mock re-verified unregressed. Screenshots `verification/wordmesh-selection-single-readout.png`, `wordmesh-selection-mock.png`. Scope kept to selection/panel/fetch-guarding; rendering, edges, search, locomotion, and honest connection states untouched. **Not verified:** on-device; a real duplicate-symbol snapshot.

- 2026-07-10: Label corruption during movement (Bug A) + Android can't move (Bug B). Measured both before touching code; Bug A's real cause was NOT the predicted one.
  **Bug A — the described mechanisms (stale camera matrix, throttled overlap, one-frame lag, duplicate sprites) are ALL absent in this build.** Scripted fly-throughs (desktop + mobile viewport, yaw sweep + forward drift) measured: matrix staleness at the label-loop site = 0 (both still and moving — `camera.getWorldPosition` refreshes matrixWorld, and matrixWorldInverse was already current), max label→node lock error = 0px, on-screen overlaps among visible labels = 0, duplicate visible words = 0. Placement + overlap already run every frame on fresh positions. **The actual cause, found by spying on WebGL calls during motion: resizing a label sprite's `CanvasTexture` canvas.** `paintLabelAutoFit` sets `cnv.width/height` to fit each new word; the near-label set rebinds every 350ms, so during motion canvases resize constantly. three re-uploads a resized canvas-backed texture via `texSubImage2D` against the OLD (smaller) GPU storage → `GL_INVALID_VALUE: glCopySubTextureCHROMIUM: Offset overflows texture dimensions`; the upload is **rejected** and the sprite keeps its previous glyphs while already rebound to a new node — the doubled / detached / mashed-text. Measured pre-fix: 13 canvas resizes → 5 `texSubImage2D` calls, **3 errored, all from a canvas source, `texImage2D` never called** (three never re-allocates). Fix: in `paintLabelAutoFit`, when the canvas dimensions change, `dispose()` the texture and create a fresh `CanvasTexture` (+ `material.map` swap) so three does a full `texImage2D` allocation at the new size. Post-fix, same motion: 31 resizes, **0 texSubImage2D errors, 0 GL warnings**, labels render crisp and locked. (Same latent bug exists in `statusChip`/keyboard canvases if they ever resize, but those are out of this task's labeling scope and rarely change size — noted, not touched.)
  **Bug B — Android couldn't move because the mobile controls were gated off.** `const isCoarsePointer = matchMedia("(pointer: coarse) and (not (pointer: fine))")` hides the joystick + vertical buttons whenever a device ALSO reports a fine pointer — true on many Android phones (S-Pen/stylus support, a paired mouse, or Chrome's pointer heuristics). Drag-look isn't gated, so the user could look but never move — exactly "can't move." Logic table (old vs new gate): desktop-mouse false/false (no regression), clean-Android true/true, **Android+fine false→true (the fix), Android fine-primary false→true**, touch-laptop false→true. Fix: `isCoarsePointer = (navigator.maxTouchPoints||0) > 0 || matchMedia("(pointer: coarse)").matches`. Also added `touch-action: none` to `#joystick-wrap` — without it Android Chrome can read a thumb-drag as a scroll/pan and fire `pointercancel` mid-drag, stopping the stick (a second failure mode). Verified with CDP touch emulation (`maxTouchPoints:5`): joystick shows, `touch-action:none`, a synthesized forward drag moves the rig 0.94 units and stops on release; desktop (0 touch points) keeps it hidden. The VR/AR path (`pollVRControllers`, xr-standard `axes[2]/[3]`) is independent of this gate and unchanged — Quest controller sticks were never gated; and since `#joystick-wrap` isn't inside `#hud` (so `#hud.in-xr` doesn't hide it), fixing the gate also surfaces the joystick in phone-AR with dom-overlay. Did NOT add AR touch-to-move as a new feature (out of scope). **Awaiting the user on a real Android phone: Screen + AR walk; and Quest VR stick if that was the "VR" meant.**
  **Verified with pixels/measurement** (headless Chrome+CDP, mobile viewport): `verification/wordmesh-labels-clean-motion.png` (crisp locked "ETH" label mid-motion). Scope kept to labeling + input; rendering, edges, selection panel, and data untouched; desktop mouse-fly and tap-select not regressed. **Skill fold-in deferred to after the Android device confirm:** Bug B → `webxr-threejs` (touch-locomotion gate + touch-action), Bug A → `threejs-visual-polish` (never resize a canvas-backed texture; recreate it).

- 2026-07-10: Mobile gyro free-look was inverted left/right (Task 1); comfort-fade investigated, left in place (Task 2).
  **Gyro yaw sign was negated.** In the `deviceorientation` handler, `yaw = gyroYawBase - degToRad(dAlpha)`. Turning the phone right decreases alpha (right-hand rule about world-up), and a right turn must *decrease* yaw (yaw>0 turns the view left under the `camera.rotation.set(pitch, yaw, 0, "YXZ")` euler), so the negation made turn-right look left. Measured pre-fix with scripted `deviceorientation` events: alpha −20° (turn right) → forward.x −0.342 (looks LEFT). Fix: `+ degToRad(dAlpha)`. Post-fix: turn right → forward.x +0.342 (right), turn left → −0.342 (left), and touch drag-right → forward.x +0.383 (right) — gyro and drag now consistent. Pitch was already correct (tilt top away → beta up → look up, forward.y +0.342) and left untouched. Scope note: only portrait was addressed; full `screen.orientation` compensation (landscape axis swap) was deliberately not added for this small fix — flag if the user reports landscape issues.
  **Comfort fade — report (not removed, per request).** It is the classic VR "blink/fade" teleport: when you pick a search result with comfort fade ON, instead of the quick-fly `teleportAnim` sliding the rig across the mesh, `fadeAnim` fades a black plane to opaque (0.2s), jumps `rig.position` to the target instantly, then fades back (0.2s) — avoiding the vection that causes motion sickness in VR. It ONLY triggers on search-result teleport, never on normal joystick/WASD flying. Added 2026-07-09 in commit 67841f7 ("Add in-scene search + laser keyboard + teleport"), bundled with teleport and bound to the left VR controller's X button; the user never asked for it specifically. The Screen-mode HUD toggle (`#btn-comfort-fade`, persisted in localStorage) is largely **dead weight**: vection nausea is a VR/AR concern, so on a flat screen the fade just briefly blacks out during a teleport with no comfort benefit. Recommend keeping the feature for VR but hiding the toggle in Screen mode — awaiting the user's keep/remove decision.
  **Verified** with scripted `deviceorientation`/pointer events (headless + `TOUCH=1` emulation) asserting yaw/pitch/forward-vector signs, since direction can't be seen in a static screenshot. Scope: input/look only; nothing else touched. **User to confirm on a real phone** that turning right looks right.

- 2026-07-10: Comfort fade removed entirely, per user ("i NEVER requested that… i HATE this feature… REMOVE it from everywhere"). It was the blink/fade teleport alternative (fade to black → jump rig → fade back) added 2026-07-09 in commit 67841f7 (the search/keyboard/teleport batch), bundled with teleport and bound to the left VR controller's X button — never separately requested. The user's stated intent was VR-only, off-by-default, behind an optional menu; no such menu exists, so it was removed from VR too rather than left as a bare toggle. Removed: the `#btn-comfort-fade` HUD pill, `comfortFadeEnabled` + `wm_comfort_fade` localStorage, `setComfortFade`, the `fadeMat`/`fadeMesh` black plane, the `fadeAnim` animate branch, the VR left-X binding (`leftXClickPrev`), and the `__wm` exposures. Search-result teleport now ALWAYS uses the smooth quick-fly (`teleportAnim`); the teleport feature itself is unchanged. Verified in-harness: HUD button gone (DOM + screenshot shows only Fly/Free-look), no `fadeAnim`/`comfortFade`/fade-plane remain, no console errors, and a search-result teleport completes end-to-end (rig moved 12.49 units, panel opened) with no fade. Gyro free-look sign fix (aee011e) remains in place.

- 2026-07-10: Android AR — add exit + fix movement; search fly-to made full-3D. Three issues from device use.
  **(1) No way to exit AR.** The mode-switcher (its "Screen" button ends the session) lives inside `#hud`, which is `display:none` in XR (`#hud.in-xr`), so in the AR dom-overlay there was no exit control. Added a standalone `#btn-exit-xr` ("Exit AR") *outside* `#hud` (so `.in-xr` can't hide it), shown only on AR enter, hidden on session end, calling `currentSession.end()`. Verified: exists, not a child of `#hud`, hidden by default, and stays visible (`display:block`, on-screen) while `#hud` is hidden.
  **(2) Can't move in AR.** The `renderer.xr.isPresenting` movement branch consumed only `pollVRControllers` gamepad axes — handheld AR has no gamepad, so the rig never moved (only node-tapping worked). Added the on-screen joystick (`joyVec`, same deadzone/ease as Screen) and the vertical buttons (`mobileVertical`) into that branch, camera-relative via `_fwd`/`_right` (the device pose). Additive and harmless in Quest VR (touch joystick idle). Moving `rig.position` is the standard WebXR locomotion offset — the device pose is applied within the rig each frame — so it translates the AR view. Verified statically (branch now references joyVec/mobileVertical); runtime AR is the user's device gate.
  **(3) Search fly-to was horizontal-only.** `computeTeleportTarget` zeroed the direction's y and locked `target.y = rig.position.y`, so a node far above/below left you at its horizontal position but your original altitude ("flew to the area, not the node"), and the camera never turned, so a node behind you ended up behind you. Now full 3D: target = `node.pos + normalize(rig.pos − node.pos) * 8` (approach from your own side, arrive 8 units out, any direction), and the teleport lerps `yaw`/`pitch` to look at the node (`lookAnglesTo`, matching the YXZ euler; shortest-path yaw so a behind-you node turns the near way). Orientation lerp is Screen-mode only (XR orientation is device-owned). Verified end-to-end for targets placed behind (+Z), far below (y−55), far above (y+60), and behind+below: all arrive at distToNode = 8.0, forward·toNode ≈ 0.98–1.0 (dead ahead), node in view; behind-case yaw turns ~180°.
  **Verified** in headless (teleport fully; AR exit via DOM/`.in-xr` simulation; AR movement statically). Scope: XR session + locomotion + teleport only. **User to confirm on a real Android phone:** Exit AR button works, joystick/up-down move the rig in AR, and search-fly lands on nodes in any direction.

- 2026-07-10: **Progress-locking tag convention (standing process).** Every shipped round is tagged with an annotated git tag `step-YYYYMMDD-N-shortname` at its commit and pushed, so any step is a one-command rollback (`git checkout step-…` / `git reset --hard step-…`). Retro-tagged this conversation's milestones: `-1-spawn-camera` e1c7e4b, `-2-frustum-spheres` 627b7dd, `-3-edge-bounds` 8f770a0, `-4-tap-and-labels` 945dabd, `-5-node-identity` f89dcbd, `-6-labeltex-android` c7c655f, `-7-gyro-freelook` aee011e, `-8-remove-comfortfade` 89f0297, `-9-ar-move-and-flyto` 3743193. Tag each future round the same way.
  **Keyboard "greyed letter bottoms" — NOT the resize bug; a depth/transparency artifact.** The key-cap label canvases are a fixed 96px, painted once, so the resize-under-old-CanvasTexture trap doesn't apply. Real cause (confirmed): the key boxes are semi-transparent (opacity 0.85) and write depth, and each billboarded letter sprite sits only KEY_SIZE*0.02 in front of a box face while being KEY_SIZE*0.7 tall — so any view tilt swings the sprite's lower half behind the box plane, where `depthTest:true` discarded it and the dark box showed through ("greyed bottom"). Fix: `depthTest:false` + `renderOrder 3` on the key-label sprite material. Verified: letters render fully even where nodes pass behind the keys.
  **statusChip DID have the resize bug** (its canvas re-sizes to fit each connection-state string) — applied the same recreate-CanvasTexture-on-size-change fix as node labels. It's XR-only now, so it only bit when the connection state changed mid-headset-session; latent but real. Verified the texture is recreated on a size-changing repaint with no throw.
  **Time-window analysis (report — no change made; user decides).** The token record has **NO volume field at all** — the card's "vol24h" is a fabricated proxy, `trending_score * 1e5`, not real volume. The per-token sparkline endpoint `/api/tokens/{addr}/transactions` returns a **fixed ~48h of hourly OHLC candles** (49 candles even at limit=1000), so a 7d/30d per-token curve is **not available** from it. The raw global feed `/api/transactions?includes=token` goes back **~22 days** (600 txns ≈ 22d), so a longer series is only buildable by paginating that global feed and bucketing per token client-side. BUT widening the window won't help the flatness: platform-wide volume is ~27 txns/day across ALL tokens, and individual tokens have single-digit lifetime txns (DWEBCAMP: 2), so a 7d/30d per-token curve would be just as flat — there simply aren't more trades. Recommendation: don't widen the per-token window; instead (a) fix the misleading "vol24h" (compute real 24h volume from the raw feed, or relabel it "trending"), and (b) if a livelier chart is wanted, aggregate the raw feed into a mesh-wide daily activity series — which belongs with the queued activity-feed/co-mention phase, not the per-node card.
  **Live token updates (report — no change made; user decides).** The mesh fetches the token list **ONCE at load** (`connectDataSource` → `getSnapshot`); the WebSocket only delivers `token-transaction` trade pulses on existing nodes and never adds tokens. So no periodic refresh, and new tokens never appear. Worse: the app query `order_by=trending_score&limit=100` returns a **server-curated ~51** that is NOT top-51-by-score — 24+ tokens with HIGHER trending scores are excluded, and the user's **DWEBCAMP does exist** (created 2026-07-09, sale ct_28dizdg…, trending 0.039, rank 411) but isn't in the curated set. To show it / guarantee new tokens: (a) switch off `order_by=trending_score` to an inclusive order — `created_at DESC` would include DWEBCAMP but flood the mesh with brand-new empty tokens, `market_cap DESC` still wouldn't (DWEBCAMP rank ~411 by cap); (b) targeted-include specific tokens by sale_address/creator and merge into the set (cheapest reliable way to show DWEBCAMP); (c) add a periodic re-fetch to pick up new tokens + live market caps — but that must rebuild nodes while preserving positional identity [[wordmesh-node-identity]] and the open selection, so it interacts with the co-mention/activity phase. Each fetch is 1 request; covering rank-411 needs pagination (limit is capped at 100). Implementation deferred to a decided follow-up step.
  **Scale / "put ALL tokens on the map" analysis (report — no change).** Total tokens = **54,152** (`meta.totalItems`, 2026-07-10). Creation rate is slow: **0 in last 24h, 9 in 7d, 50 in 30d** (~1.6/day). **83% have zero transactions** (inert). (a) Data-fetch is the first blocker: `limit` is hard-capped at 100 (250+ → HTTP 400), so the full list = **542 paginated requests, ~247 MB of JSON** (~4.5 KB/token), ~12–15 min sequential — **not viable client-side at load**; needs a backend/CDN snapshot (a trimmed list) or loading only a subset. (b) Render-all-as-spheres is the second blocker: 54k × the 24×16 sphere (~768 tris) ≈ **41M tris/frame** plus 54k emissive/bloom sources = fill-rate death — impossible on Quest (72fps) / mobile (60fps), rough even on desktop. Node rendering is already a single InstancedMesh (1 draw call) and labels are capped (top-K + near = 21, independent of total), so the ceiling is triangles/fill + fetch, not draw calls. Viable path is **tiering**: keep an "active" tier (tx_count>0 / holders — realistically hundreds to low-thousands) as instanced spheres, and render the ~45k dormant tokens as a single **Points cloud** (the far-LOD Points layer already exists — 1 draw call, ~1.3 MB, trivially cheap like a dense starfield). k-NN edges are O(n²) naive (2.9B ops at 54k) → would need a spatial grid, or better, only edge the meaningful subset. (c) "Interconnect by common sense" **is exactly the queued co-mention edge phase** (co-mention backbone + lexical fallback on the meaningful subset) — connecting all 54k by proximity is neither "common sense" nor affordable; frame it there. (d) Instant new-token + map-wide "birth spark": detection via polling `created_at DESC page 1` every 30–60s is 1 request/interval and, at ~1.6 new tokens/day, is effectively instant to a user; a `token-created` Socket.IO event (the `token-transaction` socket exists) is **unverified** — worth a cheap `socket.onAny` listen test, with polling as the guaranteed fallback. A birth spark (oversized bright radial flare, reusing the pulse system) **fits the visual grammar** — "events get animation, structure stays quiet," and a token birth IS an event — and node insertion must append at the next positional index to preserve identity [[wordmesh-node-identity]]. Effort (S=hours, M=a phase, L=multi-phase/backend): count=done; render-tiering=M, but the full-fetch behind it=L (backend snapshot) unless a subset is accepted; interconnect=the co-mention M-phase; instant-appearance polling + birth-spark=S–M; socket-event verify=S.

- 2026-07-10: **Presence / embodiment phase — design proposal (NOT built; awaiting user approval).** Goal: `.chain` users embodied in the mesh, seeing each other, able to tip-ping each other AE. Nearly all of it is reusable from the sibling **xr-stage** repo (`/Users/dev/Documents/xr-stage`, the "nostr xr stage"), which already has this architecture with clean Nostr→swap seams.
  - **(a) Wallet login stays CLIENT-SIDE.** aeternity's aepp-sdk connects to Superhero Wallet (browser-extension postMessage; mobile app via deeplink), yielding the user's `ak_…` address + signing/spend, keys never leaving the wallet — no backend needed for auth. `.chain` name resolution is free from Superhero's own API: `/api/accounts` records carry `chain_name` (address↔name), and `/api/tips` sender/receiver embed it too. Reuse `xr-stage/web/src/wallet/wallet.js` (mock with real-shaped connect/getBalance/pay/onEvent surface) — swap Lightning/NIP-57 → AE spend. Effort M; risk = aepp-sdk weight + mobile deeplink UX. Degrade: spectator mode (no login → see others, no identity/tips).
  - **(b) Presence relay = the FIRST real backend** (static-client thesis ends here: mutual live visibility needs a shared channel). Reuse `xr-stage/web/src/state/presence.js` (6 Hz `{p:[x,y,z],yaw}` heartbeat, 4 s stale-prune, smoothing) + `room/avatars.js` `AvatarPool` verbatim; only swap the transport. Two options: (1) **minimal custom WS relay** — one tiny Node process, one room, broadcast heartbeats, drop on disconnect (~S to write; recommended, no third-party, no voice needed); (2) reuse xr-stage's LiveKit + stateless token-server (`server/server.js`+`token.js`: secret in env, short-lived JWT, CORS-locked — the secret-backend-split model) if voice is ever wanted. **Backend-free degraded fallback**: "active ghosts" — spawn ephemeral avatars at nodes when an address appears in the live `token-transaction`/`/api/tips` feed; NOT true co-presence (no live movement/mutual position), be honest about that. Effort M + **USER DECISION: relay hosting/who-pays** (ties to the monthly-sponsorship model).
  - **(c) Avatars = directly reusable.** `xr-stage/room/avatars.js` is the pill/capsule body + **vertically-cut-sphere head** (`HEAD_CUT_DEG=128`, off-centre flat-cut sphere = exactly the user's "vertically-cut-sphere head"; flat `faceMount` disc for a profile image), `identity/keyface.js` = deterministic identicon from a key. Swap Nostr npub → AE address bytes; body self-glow colour from address; local body parented to the rig (matches wordmesh's rig). Effort S–M, low risk (self-contained THREE, clean seams); no per-avatar lights → scales to tens of users at 72/60 fps.
  - **(d) Tip-ping = fixed-amount AE spend + on-chain confirm.** Fixed (e.g. 10 AE) spend to the target's address via wallet deeplink/SDK (client-side; real money → explicit, confirmed, never automatic). **Delivery IS confirmable**: `/api/tips` returns `{sender,receiver,amount,tx_hash,created_at}` with `chain_name` — poll it (or match the socket feed) to fire the visual. Reuse `xr-stage/room/zapEffect.js` (idle-free in-world burst) reskinned to an AE beam/flicker between tipper→target — **fits the reserved beam/flicker grammar** (people/events animate; a person-to-person tip is an event, consistent with node trade-pulses). Effort M (beam itself S).
  - **Suggested sequencing:** Phase A (client-only, no backend): wallet login + your own embodied avatar + keyface + `.chain` name — ships value without a relay. Phase B (needs relay + hosting decision): presence → see each other move. Phase C: tip-ping beam + `/api/tips` confirm. **Open decisions:** relay hosting/cost; login required vs optional; fixed tip amount; mainnet real-AE vs testnet first. See [[project-wordmesh]]; avatar/presence/wallet/tip code all in the xr-stage repo.

- 2026-07-10: **Live new tokens shipped — trending + new-since-Monday, with a birth spark (`step-20260710-11-live-new-tokens`).** Chosen token set = **option (b)**: the curated trending ~51 MERGED with every token created inside a rolling 7-day window (`NEW_TOKEN_WINDOW_MS`), = **60 nodes today** (51 + 9 new-since-Monday, incl. the user's DWEBCAMP). Option (a) "all alive tokens" was rejected on hard data: ~7% of 54,152 are alive ≈ **3,900 spheres** (≈41M tris — unviable on Quest/mobile) and identifying them needs paging all 54k (542 req / 247 MB). `getSnapshot` now fetches trending + `created_at DESC` and unions in the recent ones (the trending query alone never contains new tokens); the recent fetch is in try/catch → trending-only degrade. **New tokens mid-session:** `startNewTokenPolling` polls `getRecentTokens` (`created_at DESC` page 1) every `NEW_TOKEN_POLL_MS` (45 s; creation is ~1–2/day so this is effectively instant) and births any not-yet-seen token created within the window — dedup by sale_address, window-filtered so old non-trending tokens never flood in, degrades silently on fetch failure. No verified `token-created` socket event, so polling is the channel. **`addLiveToken`** appends at the next positional index (identity invariant preserved [[wordmesh-node-identity]]), writes emissive attrs, refreshes pick bounds + edges, and triggers a **birth flash** (`n.birth` 1→0 over `BIRTH_DURATION` 2.6 s, driving a big scale + very-bright emissive boost applied OUTSIDE the normal flare clamp in `refreshInstances`, plus radiating pulses) — a new star igniting, map-wide, on-grammar (birth = event). Capacity guard: skips if `nodeData.length >= NODE_COUNT` (220; at ~1.6/day a session never hits it). **Verified (Screen, live):** initial 60 nodes with DWEBCAMP present; a simulated arrival appends at the next index, sparks (bright enlarged sphere — screenshot), connects, labels, and its card opens on tap; poll path adds only the genuinely-new in-window token and skips duplicates + out-of-window; mock unregressed (live-only guard). Presence Phase A/B/C stays queued next.

- 2026-07-10: **Size-by-success + buy-spark research (report — NOT built; user decides; sequenced after live-new-tokens, before presence).** (a) **Metric:** the `token-transaction` payload already carries the live post-trade **`market_cap`, `total_supply`, `buy_price`/`sell_price`, `unit_price`**, AND a full updated `token{...}` (market_cap, holders_count, tx_count, trending_score) — so live size-by-success needs **no extra fetch**. Recommend **market_cap** as the size driver (it already is, via `liveNormScale(marketCap,…)`), updated straight from the socket; it rises on buys / falls on sells via the bonding curve. Caveat: today's size is normalized to the snapshot's min/max, so a live-growing node clips at the top — switch to an absolute log scale or dynamically widen the range. There is **no per-token cumulative-volume field** (only per-tx `amount.ae` + `volume`); a lifetime-volume metric would need client-side accumulation. holders_count/tx_count are available but coarse; trending_score is hourly + already drives brightness. **Design choice to flag:** market-cap-follows-reality (shrinks on sells) vs a never-shrink "success ratchet." (b) **Buy events:** the socket **already** delivers buys — `tx_type` distinguishes **buy / sell / create_community**, payload has `sale_address` + `amount.ae` (real sizes seen: 0.003–201 AE), real-time (Socket.IO push, already consumed by `flareNode` for today's pulses). Growth just needs `market_cap` + `amount` plumbed through `onDataEvent`→`flareNode` (currently only type/word/amount forwarded) — **effort S**. (c) **Live-growth animation** interacts cleanly with the systems: size is `n.scale` (refreshInstances writes it per frame already) → lerp toward the new target on a buy; label offset + near-fade are already `nodeR = scale*0.55`-aware; **picking**: `refreshNodeBounds` is NOT per-frame, so call it after a growth so the broad-phase sphere covers the enlarged node; **positional identity is untouched** (growth changes only `n.scale`, never index/pos — no invariant interaction); cap max scale so a whale-grown node doesn't overlap neighbors (spawn-shell spacing is set at creation, not re-laid-out). (d) **Spark + notification (noise control):** trades are events → animation on-grammar. Tiers: small buys keep the existing **pulse** (all trades); any buy adds **flare + grow**; a **large buy (≥ threshold, e.g. 10 AE via `amount.ae`)** gets a **big spark + in-world toast**. Notification options: (1) **billboard toast at the node** ("+70 AE ▲ BEER" floats up and fades — reuse `xr-stage/room/zapEffect.js`, in-world/VR-safe/idle-free) — recommended, **S–M**; (2) camera-corner **feed ticker** (more UI, clutter risk, opt-in) **M**; (3) just a **bigger spark**, no text **S**. Buys are ~<$1k/day platform-wide so spam is unlikely, but gate by `amount.ae` threshold + coalesce rapid same-token buys anyway. Effort totals: size-growth **M**, buy plumbing **S**, spark/toast **S–M**. See [[wordmesh-node-identity]].

- 2026-07-10: **Buy-growth shipped — nodes live-resize by market cap, loud-buy toasts (`step-20260710-12-buy-growth`).** Node radius now follows LIVE `market_cap` from every `token-transaction`: **grow on buys, shrink on sells** (living-organism feel, follows reality both ways). `liveScaleForMc(mc)` reuses the build's log-normalisation but leaves the interpolant UNCLAMPED (so a token that grows past the snapshot top keeps growing, one sold down keeps shrinking) then hard-caps to `[MIN_NODE_SCALE 0.18, MAX_NODE_SCALE 3.5]` so a runaway node never overlaps neighbours (spawn-shell spacing is fixed at creation) or vanishes. Size lives only in `n.scale`→`n.targetScale` (per-frame eased over `SCALE_LERP_TIME` 0.4 s); **index/pos are never touched → positional identity untouched** [[wordmesh-node-identity]], and structure edges stay quiet. `refreshNodeBounds()` runs while a size ease is active so taps stay accurate at the new size (verified: tap-after-growth opens the right card). Market cap + `amount.ae` are plumbed through the socket cb → `onDataEvent` → `flareNode`. **Loud buys (`amount.ae ≥ BUY_LOUD_AE` = 10)** get an extra spark + an in-world **billboard toast** at the node ("+70 AE ▲ SYMBOL", green, floats up and fades; reuses the xr-stage zapEffect pattern; fixed-size canvas so no resize-upload bug; idle-free). **Rapid same-token buys COALESCE** — the live toast's amount accumulates and its life resets (one sprite, `_toastByNode`) and the flare ESCALATES (adds, capped at `FLARE_MAX` 2.6) instead of strobing. Small buys (<10 AE) and all sells stay quiet (pulse + grow/shrink only). **Verified (Screen/live-sim):** grow-on-buy, shrink-on-sell, cap at 3.5, toast on ≥10 AE, no toast <10 AE, coalesce to one toast, tap-after-growth opens card, mock unregressed (mock events carry no market cap → no growth; its synthetic loud buys do exercise the toast, harmless in the dev-only `?data=mock`). Presence Phase A/B/C queues next.

- 2026-07-10: **VR/Quest-3 locomotion + controller interaction rebuilt to the `webxr-threejs` skill (`step-20260710-13-vr-controls`).** Device report: "can only move on 1 axis", no fly/run, no controller click-to-open, no X-exit. Fixes: **(locomotion)** `readStick(gp)` reads the thumbstick robustly — xr-standard axes[2]/[3], falling back to [0]/[1] if those are dead (the "1 axis" cause on runtimes that map the stick differently) — with a 0.15 radial deadzone; **left stick = head-relative move (both axes)**; **right stick X = SMOOTH continuous turn** (`VR_TURN_SPEED` 2.4 rad/s, replacing the old 30° snap-only); **sprint** on full-push magnitude OR **grip** (`buttons[1]`) held; **fly toggle** on right-stick-click (`buttons[3]`), fly-where-you-look (vertical comes from look pitch via `_fwd.y` when `flyMode`, default on). **(interaction)** both controllers emit a **laser line + reticle** (skill "target and select"); per-frame `updateVRHover()` raycasts both, trims each laser to its hit, and highlights the hovered node (reticle at the hit + the same `hoveredNodeIndex` desktop uses, so it gets a label); **trigger (`buttons[0]`) selects → opens the token card** via the SAME raycaster/`handleSelectRay`/`selectNode` path as mouse/tap. **(exit)** **Left X (`buttons[4]`) → `currentSession.end()`** → `onXRSessionEnd` restores Screen (VR + Quest AR; phone-AR keeps its DOM exit button); lasers/reticle hidden + `hoveredNodeIndex` cleared on exit. Skill divergence noted: the skill prefers X→pause-menu-with-exit, but the user asked for X→direct-exit — honored the user. Perf: +2 raycasts/frame (instanced broad-phase) + 2 thin lines + 1 ring — negligible for 72 fps. **Verified (code paths, synthetic XR input — headless can't enter real WebXR):** readStick robustness (both axis layouts + deadzone), 2-axis move + smooth turn + sprint (magnitude & grip), fly toggle, X-exit calls `session.end()`, controller-ray hover→reticle+highlight, trigger-select opens the aimed node's card; Screen mode unaffected (lasers hidden, tap-select works). **Needs on-device Quest 3 confirmation** of feel + real controller entry. Presence phases queue next.

- 2026-07-10: **Presence Phase A shipped — wallet login + self-embodiment, ZERO backend (`step-20260710-14-presence-a`).** Spectator is the default (login OPTIONAL — the user never answered required-vs-optional, so it doesn't gate the app; flag if wrong). A "Connect wallet" pill (flat HUD, hidden in XR) connects, gives a `.chain` identity, and embodies you. **Wallet is a MOCK seam** (real-shaped `wallet.connect()/disconnect()`, like xr-stage's `wallet.js`) — READ-ONLY (address + name), **no keys, no signing, no spend, no real-money surface this phase**. REAL swap documented inline: `@aeternity/aepp-sdk` via `walletDetector`/`BrowserWindowMessageConnection` (extension) or Superhero mobile deeplink; bundling that SDK is a deferred decision. `.chain` name resolves from Superhero's own API (`GET /api/accounts/{address}` → `chain_name`), used now; falls back to a short address when unset/unreachable. **Avatar** ported from `xr-stage/room/avatars.js` + `identity/keyface.js`: pill `CapsuleGeometry` body + vertically-cut-sphere head (`AV_HEAD_CUT_DEG=128`, off-centre flat face) carrying a deterministic **keyface identicon** derived from the AE address bytes (npub→address). Per webxr-threejs, the **local** avatar is the body only — **head culled so it never occludes the first-person camera**; the full body+head+keyface (`makeAvatar(..,{withHead:true})`) is verified and ready for **Phase B** remotes. A HUD **identity chip** shows the keyface + `.chain` name (or short address). **Verified in-harness:** spectator default (no avatar/chip), connect→embody (body parented to rig, head culled locally, chip shows keyface+identity), full avatar builder renders body+cut-sphere-head+keyface-face, keyface deterministic per address, disconnect→unembody; Screen mode unaffected. **Needs the user's real Superhero Wallet to confirm:** actual extension/mobile connect + a real `.chain` name resolving (mock address has none → shows short address). **Phase B (presence relay) — OPEN DECISION for the user: relay hosting / who pays** (the first real backend; ties to the monthly-sponsorship model). Phase C tips: **10 AE** default (confirm) + **mainnet-vs-testnet** must be answered before any real spend path. See [[project-wordmesh]]; source patterns in the xr-stage repo.

- 2026-07-10: **Presence Phase B shipped — see each other (`step-20260710-15-presence-b`).** The one backend: a tiny stateless **WebSocket relay** (`server/relay.mjs` + `package.json` + README; `ws`, one room, rebroadcasts `{t:'presence',p,yaw,name,seed}` heartbeats, `{t:'leave'}` on disconnect; holds no secrets). Client sends a **6 Hz** pose+identity heartbeat and renders inbound peers as **remote avatars** (full pill body + cut-sphere head + keyface from the peer's `seed`, + a floating name label), smoothed toward their reported pose (lerp), pruned after 4 s stale, reconnect on drop. Works in **spectator** (per-session `guest-xxxxx` id) and with a **wallet** (`.chain`/address identity, updates live when you connect). Relay URL is **configurable** (`?relay=wss://…` or `DEFAULT_RELAY`; empty = presence off/solo) so hosting can change without a code edit. **Verified with TWO headless clients over a local relay:** each renders the other's avatar, B's avatar target follows A's live movement, identity name updates when A connects a wallet, and B prunes A on leave (screenshot: a spectator seeing another user's pill+cut-sphere-head+name in the mesh). **Relay hosting options (user to pick + deploy):** Render/Railway/Fly free tier (sleeps when idle — fine for testing), ~$4–6/mo VPS (Hetzner/DO, always-on, Caddy auto-TLS), or Cloudflare Durable Object WS (near-free, needs the handler rewritten to the Workers API). For "two wallets testing today": deploy `server/` to Render (`npm start`), then open the site with `?relay=wss://<host>`. **Needs the user's two devices + a deployed relay to confirm** the real cross-device experience (local two-client is proven). Phase C tips (mainnet, 10 AE, explicit wallet-confirm) next.

- 2026-07-10: **Real Superhero Wallet login wired (`step-20260710-16-real-wallet`).** Swapped the mock for the actual `@aeternity/aepp-sdk@14`, dynamically imported from jsdelivr (`/+esm`) only on click (heavy dep, kept off initial load — verified it loads and exposes `AeSdkAepp`/`Node`/`BrowserWindowMessageConnection`/`walletDetector`). Canonical aepp flow: `walletDetector(new BrowserWindowMessageConnection())` → `aeSdk.connectToWallet(newWallet.getConnection())` → `subscribeAddress("subscribe","connected")` → active `ak_…` address; **mainnet** (`https://mainnet.aeternity.io`); **READ-ONLY** (address + `.chain` name via `/api/accounts/{address}`), no signing/spend this phase. Graceful failure: 5 s detector timeout → button "No wallet found", app keeps working (verified in headless: SDK imports, no extension → clean error, no uncaught throw, app alive). `?mockwallet=1` forces the deterministic demo identity (headless/test + wallet-less demo) — that path still embodies + shows the chip. **CANNOT verify without a real wallet:** the actual extension/mobile handshake and that the exact v14 SDK calls return the real address — the canonical pattern is implemented but the SDK API is version-sensitive, so **expect possible small iteration once the user tries his real Superhero Wallet** and reports the exact behavior/errors. **Sequencing note:** Phase C tips (mainnet, 10 AE, explicit confirm) is a real-money SPEND built directly on this wallet connection — do NOT ship it until the user confirms real login works, so the spend isn't stacked on unverified glue.

- 2026-07-10: **Presence relay live on Railway, baked in (`step-20260710-17-relay-live`).** User deployed `server/` to Railway; `DEFAULT_RELAY = "wss://wordmesh-production.up.railway.app"` (so no `?relay=` needed; the param still overrides, "" disables). **Verified the LIVE relay:** HTTPS GET → 426 Upgrade Required (correct for a WS-only endpoint), wss handshake opens ~600 ms and returns `{"t":"welcome","id":"u1"}`; the two-client presence test re-run against the live Railway wss (not localhost) — both connect, B sees A at the exact synced position, identity + prune-on-leave work; no-`?relay=` load connects to the baked default (`relayReady:true`), `?relay=` still wins. **Cold start to flag:** Railway can idle-sleep, so right after a page load the first peers may take a few seconds to appear while the WS connects; the client auto-reconnects (self-healing), so it resolves without a refresh. Phase C tips still gated on the user confirming real wallet login first.

- 2026-07-10: **Superhero Wallet connect — diagnostics + likely bugfix + Superhero-only rename (`step-20260710-18-wallet-diagnostics`).** User's real wallet connect failed (no error text yet). Checked the canonical aepp-sdk@14 flow (docs/forum) — it matched ours EXCEPT the `walletDetector` callback shape: it fires `{ wallets, newWallet }`, and `newWallet` is set only for a *freshly-announced* wallet; if Superhero announced before our scan started it's in `wallets` instead, so `newWallet.getConnection()` silently never fired — **the likely bug**. Fixed: `const wlt = newWallet || Object.values(wallets||{})[0]`. **Superhero-only**: only connects when `wlt.info.name` matches `/superhero/i` (ignores other aeternity wallets). **Staged diagnostics**: `connect(onStage)` reports each step (loading SDK → scanning → connecting to <name> → requesting address → resolving .chain) on the button, and on failure the button shows a coded label (`No Superhero Wallet found` / `Wallet rejected connection` / `SDK failed to load` / `Wallet detector error`) while `console.error` logs `code — message — cause` for a screenshot. Timeout 5→8 s. Button renamed **"Connect Superhero Wallet"**. `.chain` resolution confirmed against a REAL address: `GET /api/accounts/{address}` → 200 with `chain_name` (e.g. `吉.chain`). **Verified in headless:** rename, staged text ("Connecting… scanning for Superhero Wallet"), timeout → "No Superhero Wallet found" + full console error, mock path still embodies. **Still needs his extension to confirm the real handshake** — but now his next attempt's button+console will pinpoint the failure stage (detection vs connect/approval vs SDK load). Mobile deeplink (open-in-Superhero-app) still to do; extension + in-app-browser path covered by the detector.

- 2026-07-10: **aepp-SDK vendored same-origin — fixes desktop "SDK failed to load" (`step-20260710-19-vendor-sdk`).** User's desktop (with the Superhero extension) failed at the SDK import, BEFORE any wallet detection; mobile correctly said no-wallet. Reproduced the import on the live GitHub-Pages page in the harness — jsdelivr `/+esm` actually loads fine HERE, so the desktop failure is that user's environment (adblocker/proxy/extension/CDN variance) blocking the runtime CDN import, which we can't control. Fix (task-preferred): **vendored a self-contained bundle** — `vendor/aepp-sdk.bundle.js` = `@aeternity/aepp-sdk@14.1.1` esbuilt (`--bundle --format=esm --platform=browser --target=es2020` of `es/index-browser.js`), 1.5 MB, committed (no deploy build step). `AEPP_SDK_URL` now `new URL("./vendor/aepp-sdk.bundle.js", import.meta.url)` (same-origin, correct under the `/wordmesh/` base). Verified: the bundle imports in-browser AND `new Node()/new AeSdkAepp()/new BrowserWindowMessageConnection()` construct with no Node-global errors; the real app's connect now reaches "scanning for Superhero Wallet" (SDK load no longer fails). Mobile no-wallet message is now device-aware: touch → **"Open in Superhero app browser"** (vs desktop "No Superhero Wallet found"). **Still needs his extension to confirm the detection+approval handshake** (headless has no extension) — but "SDK failed to load" should be gone; his next desktop attempt should reach the Superhero approval popup (or a detection/approval error we can then pinpoint). Rebuild recipe in `vendor/README.md`. NOTE: my Node syntax-lint can't parse `import.meta` (module-only) — the browser load is the real syntax check here.

- 2026-07-10: **Any Superhero Wallet connects (± .chain) + long-name truncation (`step-20260710-20-wallet-names`).** Confirmed: any Superhero Wallet connects regardless of a `.chain` name — `resolveChainName` returns `null` (never throws) on a 400/missing/timeout, and both `embody` and `presenceIdentity` fall back to a short `ak_…6…4` address; no name never blocks embodiment (verified: mock address → 400 → chainName null → connected+embodied, chip shows short address). Added `shortName(s, max)` — truncates long `.chain` names for display (identity chip `max 18`, 3D avatar name labels `max 20`) with an ellipsis; the FULL name still travels over the presence wire, only the render is shortened (verified: `bob.chain` unchanged, a long name → `this-is-a-really-…`). Token node labels already truncate via `displayWord`. Wallet-`.chain` login itself still needs his extension to confirm end-to-end.

- 2026-07-10: **Real wallet CONFIRMED on desktop** (user: vendored SDK + detector fix did it — Phase C gate is passed). **Mobile wallet guidance (`step-20260710-21-mobile-wallet`).** Superhero mobile has no injected provider in a normal Safari/Chrome — the only working path is the **Superhero app's own in-app Browser**, where the same `walletDetector` postMessage bridge fires. So on mobile, a failed connect now shows a help panel: "open the Superhero Wallet app → its Browser tab → open this page," with **Copy link** (clipboard) + **Open Superhero app** (`superhero://` = the wallet's registered `MOBILE_SCHEMA`, best-effort launch) + Dismiss. Verified: panel shows on touch after the no-wallet timeout, hidden on desktop. Honest limits: **iOS + Android both require the in-app browser** (or a full `superhero://` deeplink-connect / WalletConnect flow, which is a larger untestable effort — deferred); a plain mobile browser can't reach the wallet. Needs his phone to confirm the in-app-browser detector actually connects.

- 2026-07-10: **Presence Phase C shipped — tip a person 10 AE (`step-20260710-22-tip-beam`).** Tap an embodied peer → an explicit **"Send 10 AE to <name>?"** confirm (subtitle "A mainnet transaction — your wallet will ask you to sign") → the wallet signs a **mainnet** spend (`aeSdk.spend(10, address, {denomination:'ae'})`, **never automatic**) → a tipper→target **beam** fires (green line + traveling spark, fades ~1.4 s, on the event-animation grammar) and the recipient gets a "Received 10 AE ✦" toast. Only **wallet-identified** peers (seed starts `ak_`) can be tipped — spectators (guest ids) can't receive AE; tipping requires your own wallet. Targeting: `pickTipTarget` raycasts remote-avatar groups in `selectFromRay`, winning over a node only if nearer. Tips are **relayed** (added a `tip` type to `server/relay.mjs`) so the recipient + onlookers see the beam; each client fires the beam between the from/to avatars, and `to===mySeed` shows the received toast. Fixed a real bug found in test: `upsertRemote` now updates a peer's **seed** (+ repaints the keyface) when they connect a wallet mid-session (guest→`ak_`), so tips route to the real address. Mock address is now unique per load (two demo clients are distinguishable). **Verified with two headless clients over a local relay (mock sign):** A tips B → A shows the beam + "Sent 10 AE ✓", B receives the beam + "Received 10 AE from … ✦"; confirm dialog screenshot. **NEEDS: (1) the user redeploy the Railway relay** (it now handles `tip` — the tipper's own beam works regardless, but peers only see it after redeploy); **(2) his two real wallets + real AE** to confirm the actual mainnet `spend` (only the sign step is mocked in-harness). 10 AE is small per his call; still every tip is an explicit wallet-signed action.

- 2026-07-10: **Mobile wallet connect via Superhero deeplink — regular browser → app → back (`step-20260710-23-mobile-deeplink`).** User clarified: a normal mobile browser must hand off to the Superhero **app**, not require the in-app browser. Found the real mechanism in `superhero-wallet/docs/deep-link-schema.md`: a **universal-link callback flow**. Connect = navigate to `https://wallet.superhero.com/address?x-success=<callback with {address}&{networkId}>&x-cancel=<callback>` — opens the app (installed) or PWA, user approves, redirects back to our page with `?address=ak_…&networkId=…`. Implemented: on mobile (`isCoarsePointer`), `wallet.connect()` navigates to `buildConnectDeeplink()` (never resolves — page unloads); a load-time `handleWalletCallback()` reads `?wcb=addr&address=` on return → sets address, resolves `.chain`, embodies, scrubs the URL. Desktop unchanged (extension via `walletDetector`). **Verified in-harness:** deeplink URL matches the schema (host `wallet.superhero.com`, `/address`, `{address}`+`{networkId}` placeholders, points back, has `x-cancel`); the return callback (`?wcb=addr&address=ak_…`) connects + embodies + scrubs the URL. **Needs his phone** to confirm the app actually opens + redirects back (the return is simulated in-harness). Also documented (same doc): `/sign-transaction?transaction=tx_…&broadcast=true&x-success=<cb {transaction-hash}>` — the path for **mobile tip-SENDING**, a follow-up; for now mobile connect gives an address (be seen, receive tips) but tip-sending shows "desktop-only for now" (no live SDK from the deeplink connect).

- 2026-07-10: **Staging environment live.** Since the production link (`developerofwebxr-oss.github.io/wordmesh/`) is going public, new/risky phases get tested on a staging clone first. Created repo **`developerofwebxr-oss/wordmesh-staging`** (public), pushed `main`, enabled Pages via `gh api -X POST repos/.../pages -f build_type=workflow` → **https://developerofwebxr-oss.github.io/wordmesh-staging/** (deploy green, verified: 65 live tokens load, vendored SDK 200 same-origin, connects to the relay — all paths are `import.meta.url`-relative so the different base path just works). A `staging` git remote is set on the local repo. **Recommended flow:** develop → `git push staging main` (deploys staging) → user tests the staging link → when good, `git push origin main` (deploys production). Chose a separate repo over a same-repo branch+subpath deploy: GitHub Pages serves one artifact per repo, so two live branches would need a custom combined-artifact workflow — a clone is simpler and fully isolates production. **Shared with production (by design):** the same Railway relay (`wss://wordmesh-production.up.railway.app`) and the live Superhero APIs — so two people on staging see each other, and staging presence/tips mix with production's (same relay room). If that ever matters, give staging its own relay via `?relay=` or a separate `DEFAULT_RELAY`.

## Facts that shape tone/claims (verified 2026-07)
- Business model: monthly sponsorship, NOT grants/bounties. Aeternity/Superhero sponsor #1. Pitch after phase 0 ships. Never grant/bounty language.
- Each phase doubles as a monthly deliverable. Superhero case exclusively. AE ≈ $0.006–0.008, volume <$1k/day (hence replay). Superhero mobile app newly launched; QR handoff stars it; store links carry attribution.

## Sponsor-reporting instrumentation
- Phase 0+: tag outbound store/app links `?ref=wordmesh`; privacy-light visit counter. Phase 2: register project `.chain`. Phase 4: economy revenue report. Every phase ships a 20–30s clip.

- 2026-07-12: **CRITICAL fabrication fix — tip beam now gated on a REAL signed tx (`step-20260712-24-tip-real-sign`).** User's two-wallet test: "I pressed send 10 AE but it didn't ask wallet to sign" — the beam + relay fired on a MOCK/fabricated event with no mainnet tx (violates the never-fabricate-events guardrail). **Two root causes fixed.** (1) **`confirmTip()` gate**: previously the beam fired unconditionally after `await spend()` (with a `txHash || ""` fallback) and relayed regardless. Now: shows **"Waiting for wallet to sign…"** first; requires a real signer (`wallet._sdk`) or bails with "Reconnect your wallet (no signer)"; calls `aeSdk.spend(10, addr, {denomination:'ae'})`; extracts the hash and **throws unless it matches `/^th_/`**; the tipper→target **beam + relay fire ONLY inside the success path after a valid `th_` hash**. Reject/timeout/no-hash → **"Tip cancelled — nothing sent", NO beam, NO relay**. (2) **connect routing**: the old `else if (isCoarsePointer)` branch routed touchscreen laptops (which HAVE the extension) straight to the mobile deeplink → a connect with **no `_sdk` signer**, so tips could never sign — the likely "didn't ask to sign" cause. Now the extension `walletDetector` is tried **first on every device** (4.5 s timeout) and the deeplink is a fallback **only** inside the catch when `no-wallet && isCoarsePointer`. (3) **Mock is local-only**: `?mockwallet=1` still fires a LOCAL beam for demos but **NEVER relays** (`if (!isMock && relaySocket…)`), so a mock tip can't appear on anyone else's screen as a fabricated event. **Verified in-harness (stubbed `_sdk.spend`, gate ordering proven):** real→`th_` hash = beam + relay(w/ txHash) ✓; real→no hash = no beam/no relay + "nothing sent" ✓; real→reject = no beam/no relay ✓; no-signer = no beam + "reconnect" ✓; mock = local beam, **relayTipsSent 0** ✓. **NEEDS his two real wallets:** confirm that pressing **Send 10 AE now opens the Superhero sign popup** and the beam appears ONLY after he approves (real `th_` on-chain) — the sign step is the one thing headless can't exercise. Shipped to **staging first** per the public-launch flow.

- 2026-07-12: **iPhone staging connect fix — mobile deeplink must fire IN the click gesture (`step-20260712-25-ios-gesture-deeplink`).** Device report: Android wallet-connect works on both staging and production; iPhone works on production but on **staging opens `wallet.superhero.com` in the browser instead of the Superhero app**, so tips couldn't be tested. Root cause = a per-timing regression introduced by step-24's connect-routing change, NOT a per-origin difference. **Production (pre-step-24)** navigates `location.href = buildConnectDeeplink()` **synchronously inside the click handler** (top-level `else if (isCoarsePointer)`, no `await` before it) → iOS treats it as user-initiated → opens the app. **Staging (step-24)** made coarse-pointer fall through to the desktop branch, which `await`s the SDK load + a **4.5 s** extension-detector timeout and only then navigates from an async `catch` → **outside the user gesture** → iOS's universal-link handler declines to open the app and loads the web URL instead. Android's Chrome doesn't enforce that user-gesture rule, so it worked on both (hence the false "per-site" appearance). **Fix:** added `isMobileDevice` (UA test: `/iPhone|iPad|iPod|Android/i` + iPadOS-desktop-UA via `MacIntel && maxTouchPoints>1`) — a true phone/tablet, where **no** Superhero browser extension can exist. `wallet.connect()` now routes mobile devices to the deeplink **synchronously in-gesture** (restoring production's behavior) BEFORE the desktop `else` that runs the extension detector. **Touchscreen laptops keep step-24's fix**: they report a desktop UA → `isMobileDevice` false → extension detector → real `_sdk` signer (so `isCoarsePointer`-but-not-mobile still uses the extension, the exact case step-24 protected). The catch-branch deeplink fallback stays for the rare non-mobile coarse device (touch desktop w/ no extension). **Verified in-harness (CDP UA override + `wallet.superhero.com` nav-blocked so the context survives):** iPhone UA → `isMobileDevice:true`, first stage "opening Superhero Wallet app", **`wm_wallet_pending` set synchronously** (proves in-gesture nav), SDK never loaded; Android UA → identical; Windows desktop UA → `isMobileDevice:false`, first stage "loading SDK", deeplink never taken. `confirmTip` gate (step-24) unchanged. **NEEDS his iPhone on staging:** confirm Connect now opens the Superhero **app** (not the web page); then the laptop(sender)+Android(receiver) two-wallet tip test can run on staging.

- 2026-07-12: **Mobile tip SENDING shipped — sign 10 AE via the Superhero app deeplink (`step-20260712-26-mobile-tip-send`).** Desktop→Android real mainnet tip is CONFIRMED by the user (beam fired, 10 AE moved). This adds the reverse: a phone (iPhone/Android) can now SEND a tip. A regular mobile browser has no live signer (`wallet._sdk` is null after the deeplink connect), so `confirmTip` used to bail "no signer". New mobile path: **build an UNSIGNED spend client-side, hand it to the Superhero app to sign+broadcast, get the on-chain hash back**, then the SAME `th_`-hash gate fires the beam/relay. Pieces: (1) `buildUnsignedTip(recipient)` = `buildTxAsync({tag:Tag.SpendTx, senderId:wallet.address, recipientId, amount:10e18 aettos, onNode:new Node(mainnet)})` — auto-fills nonce/fee/ttl from the node (verified against mainnet: builds a real `tx_`, tag 12, amount 10000000000000000000, live nonce+fee). (2) `buildTipDeeplink(tx)` = `wallet.superhero.com/sign-transaction?transaction=<enc tx>&broadcast=true&x-success=<cb ?wcb=tip&th={transaction-hash}>&x-cancel=<cb ?wcb=tipcancel>` — same schema/placeholder-substitution shape as the working connect deeplink. (3) **Two-step, iOS-gesture-safe:** tapping "Send 10 AE" runs `startMobileTip` which builds the tx (async node round-trip) and relabels the button to **"Open Superhero Wallet →"**; the SECOND tap navigates **synchronously within that fresh gesture** (iOS only opens the app for an in-gesture navigation — the same rule that bit the connect flow in step-25; building the tx first would break a one-tap gesture). (4) Return handler: `?wcb=tip&th=th_…` → `completeMobileTip` (toast "Sent ✓", relay so the receiver+onlookers draw the beam — retries until the relay reconnects on the reload, and draws our own beam once presence re-renders the target); **empty/garbage hash → "Tip failed — nothing sent"; `?wcb=tipcancel` → "Tip cancelled" — no beam, no relay** (gate preserved). Desktop unchanged (live `_sdk.spend`). **Verified in-harness (iPhone+desktop UA, real mainnet tx build):** deeplink shape matches schema; two-step build produces a real `tx_` and stashes pending context; real `th_` return → beam+relay(with hash)+toast; empty-hash/cancel → no beam/relay; desktop gate (valid→beam, reject/no-hash→nothing) unregressed. **NEEDS his two phones:** (a) iPhone connect opens the app (step-25); (b) a phone→phone tip — tapping "Send" then "Open Superhero Wallet →" opens the app to a **10 AE sign**, and on approve the beam fires on the receiver. **One thing only the real app can confirm:** that it returns the hash in the `{transaction-hash}` placeholder (I mirrored the proven `{address}` connect mapping, but the exact sign-return token is the last unverifiable bit). Shipped to STAGING.
